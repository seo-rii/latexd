name: immediate-write-stack
summary: Wrapper class/package stack uses plain `\write` and `\immediate\write` non-visibly without leaking aux-write payload into visible output, even across a later option-only revision.
