# latexd Work Backlog

이 문서는 `latexd`의 남은 작업을 한 번에 보기 위한 backlog다.  
기준 문서는 [`README.md`](/home/seorii/dev/hancomac/latexd/README.md) 와 [`PROGRESS.md`](/home/seorii/dev/hancomac/latexd/PROGRESS.md) 이고, 여기서는 그 내용을 실행 가능한 작업 묶음으로 다시 정리한다. 더 자세한 `M12` 세부 체크리스트와 완료 기준은 [`docs/m12-checklist.md`](/home/seorii/dev/hancomac/latexd/docs/m12-checklist.md) 에 둔다.

## Status Key

- `Done`: 이미 구현/회귀가 있는 작업
- `Future`: 지금 당장 호환 목표는 아니지만 추후 목표로 적어 두는 작업

## Current Focus

- `M12 arXiv hardening`은 `2026-03-27` 기준으로 완료로 본다.
- `M11`은 이제 `M11.1`~`M11.4`, `M12`는 `M12.1`~`M12.6`으로 쪼개어 관리한다.
- 현재 backlog의 우선순위는 post-`M12` follow-on 정리다.
- 따라서 지금 실제 우선순위는:
  - wider engine-profile hardening
  - artifact-driven invalidation beyond the current narrow profile
  - richer renderer/display-list ownership
  - stronger external editor integration

### Deferred By The Packaging Pass

- `2026-03-30` 기준으로 web frontend를 `pnpm` workspace + vanilla-TS `viewer-core` + SvelteKit app shell로 재구성하는 작업을 먼저 끝냈다.
- 이 작업 때문에 원래 이미 다시 진행 중이던 post-`M12` follow-on이 잠시 멈췄고, 아래 항목들이 packaging 이후 즉시 재개할 큐로 남아 있다.
- 먼저 다시 잡을 작업은:
  - stronger external editor integration
    - open-source bridge surface와 editor hand-off path를 지금보다 더 명시적으로 고정
    - viewer/editor helper/runtime tail regressions를 계속 추가
  - viewer-core/app boundary tightening after the split
    - `viewer-core`에서 latexd-specific transport, DTO decoding, hash/global bridge, host custom-event wiring을 더 걷어내기
    - pure core 테스트와 latexd host/runtime 테스트를 계속 분리해서 유지하기
  - then resume the broader post-`M12` follow-on
    - artifact-driven invalidation beyond the current narrow profile
    - richer renderer/display-list ownership
    - wider engine-profile hardening

### Current Compiler Hardening Batches

- `2026-04-06` 기준으로 compiler hardening은 한 덩어리 backlog가 아니라 아래처럼 더 작은 배치로 쪼개서 진행한다.
- `CH-1`: `unchanged_tail` no-op matrix completion
  - pure `follows` / `precedes` parity가 아직 비어 있는 top-level + multi-input stems를 family별로 닫기
  - 각 stem에서 full reuse 여부와 `reused_checkpoint_id` / `start_checkpoint_id` contract를 명시적으로 고정하기
- `CH-2`: semantic bibliography replay/rebuild matrix completion
  - same-page vs included-body, sibling `.bbl`, dirty-order, readable/unreadable noise cross-product를 family별로 닫기
  - semantic no-op replay와 semantic-change conservative rebuild를 topology별로 같은 수준으로 맞추기
- `CH-3`: external oracle/profile matrix completion
  - `pdf_latex`, `xe_latex`, `latex -> dvips -> ps2pdf`, custom `compiler_bin`의 success/warning/failure/dep-trace branches를 profile별로 정리하기
- `CH-4`: selector/planner/detector pure-unit hardening
  - `shipout_replay_plan_`, `plan_page_patches(...)`, `find_unchanged_tail(...)`의 남은 tie/guard edge를 좁은 unit으로 닫기
- `CH-5`: smoke harness shape cleanup
  - `crates/latexd/tests/smoke.rs`는 이미 너무 커졌기 때문에, current family batch를 몇 개 더 닫은 뒤 family별 smoke 파일 또는 shared harness helper로 쪼개기
  - 같은 broad family를 계속 키우기 전에 “다음 batch를 같은 파일에 더 넣을지”를 한 번씩 재평가하기
  - `2026-04-09` progress: root `smoke.rs`는 이제 host/prelude 역할만 남기고 `smoke/external_oracle.rs`, `smoke/bibliography_features.rs`, `smoke/bibliography_output.rs`, `smoke/bibliography.rs`, `smoke/reference_variants.rs`, `smoke/unchanged_tail.rs`, `smoke/replay_selection.rs`를 `include!` 한다
  - `2026-04-10` progress: `smoke/external_oracle.rs`도 include-only host가 되었고, matrix는 `external_oracle_custom_compiler`, `external_oracle_internal_compiler`, `external_oracle_pipeline_success`, `external_oracle_engine_failures`, `external_oracle_missing_artifacts`로 다시 나뉘었다. representative exact checks와 `external_oracle` family filter는 `70/70` green이다
  - `2026-04-10` progress: `smoke/external_oracle_missing_artifacts.rs`도 include-only host가 되었고, missing-tool / nonzero-exit / missing-output / `pdf_latex` fallback matrix는 `..._binary_discovery`, `..._latex_stage`, `..._dvips_stage`, `..._ps2pdf_stage`, `..._output_files`, `..._pdf_latex_toolchain`로 다시 나뉘었다. representative exact checks와 missing/nonzero/spawn/`pdf_latex` filters는 green이다
  - `2026-04-10` progress: `smoke/bibliography_features.rs`도 include-only host가 되었고, semantic aux / citation fields / natbib / biblatex printbibliography / year-alias feature smokes는 `bibliography_features_core_aux`, `bibliography_features_citation_fields`, `bibliography_features_natbib_variants`, `bibliography_features_biblatex_print`, `bibliography_features_year_aliases`로 다시 나뉘었다. representative exact checks와 citation/natbib/biblatex/printbibliography/bibnumbered/year-suffix/citation-alias filters는 green이다
  - `2026-04-25` progress: `smoke/bibliography_features_core_aux.rs`는 이제 `crates/latexd/tests/support/bibliography_features_semantic_case_helpers.rs`의 semantic bibliography fixture/compile path를 사용하고, citation-field basic-variants wrapper도 같은 helper로 위임한다. core-aux leaf는 `124`줄에서 `79`줄로 줄었고 helper는 `68`줄이며 core exact check와 broader `variants` smoke filter는 green이다
  - `2026-04-28` progress: 같은 semantic bibliography helper가 rev1 aux-sensitive build-meta contract도 성공 compile 때 검증한다. core-aux leaf는 `79 -> 61`줄이 되었고 helper는 `86`줄이며 core exact와 citation-field basic-variant exact checks `6/6`은 green이다
  - `2026-04-29` progress: semantic-index fixture compile helpers는 이제 changed files를 string slice로 받아 support 내부에서 `Utf8PathBuf`로 변환한다. backdate leaf는 `67 -> 57`줄, includeonly는 `42 -> 38`줄, semantic-index artifact는 `55 -> 51`줄, printbibheading은 `26 -> 23`줄로 줄었고 affected exact lanes `5/5`는 green이다
  - `2026-04-29` progress: semantic-index helper가 이제 rev별 `semantic-index.json` loading도 맡는다. semantic-index artifact와 printbibheading leaves는 `51/23 -> 47/19`줄, helper는 `108`줄이며 exact lanes `2/2`는 green이다
  - `2026-04-29` progress: `smoke/bibliography_semantic_features_backdate_and_rebuild_backdate.rs`는 rev1 aux path/payload와 replay start page를 local로 잡아 repeated path/assert scaffolding을 줄였다. backdate leaf는 `57 -> 53`줄이며 exact lane은 green이다
  - `2026-04-29` progress: `smoke/bibliography_semantic_features_include_sections_and_labels.rs`도 같은 semantic-index fixture/compile helper를 재사용한다. leaf는 `60 -> 32`줄이며 exact lane은 green이다
  - `2026-04-29` progress: `smoke/bibliography_semantic_features_manual_toc_starred_sections.rs`도 같은 semantic-index fixture/compile helper를 재사용한다. smoke leaf에는 output/aux/stored-source assertion만 남았고 manual TOC starred-section leaf는 `57 -> 31`줄이며 exact lane은 green이다
  - `2026-04-29` progress: `smoke/bibliography_semantic_features_natexlab_output.rs`는 shared bibliography semantic-case fixture/compile helper를 재사용해 local manifest/world/driver setup을 제거했다. natexlab output leaf는 `51 -> 22`줄이며 exact lane은 green이다
  - `2026-04-29` progress: `smoke/bibliography_features_core_aux.rs`는 repeated semantic-aux path와 stored-source main-file indexing을 locals로 잡아 같은 join/map lookup 반복을 줄였다. core-aux leaf는 `61 -> 56`줄이며 exact lane은 green이다
  - `2026-04-29` progress: `smoke/bibliography_features_core_aux.rs`는 `sources.json` parse 직전의 별도 existence assertion을 걷어내고 read failure에 맡긴다. core-aux leaf는 `56 -> 55`줄이며 exact lane은 green이다
  - `2026-04-14` progress: `smoke/bibliography_features_citation_fields.rs`도 include-only host가 되었고, citation-field matrix는 `..._basic_variants`, `..._identifiers`, `..._metadata`로 다시 나뉘었다. targeted exact citation-field checks `14/14`는 green이다
  - `2026-04-25` progress: root `smoke.rs`에 남아 있던 공용 smoke harness prelude를 `crates/latexd/tests/support/smoke_harness_helpers.rs`로 옮겼다. `smoke.rs`는 `37`줄짜리 import/include host만 남았고, shared build-meta/source structs, PATH guard, executable script writer, fake external-tool scripts는 `230`줄짜리 support include가 맡는다. `cargo test -p latexd --test smoke --no-run`과 custom-compiler placeholder exact smoke는 green이다
  - `2026-04-09` progress: root `smoke.rs` line count는 약 `79k -> 268`까지 줄었고, 남아 있던 small root tests는 `smoke/internal_baselines.rs`로 이동했다
  - `2026-04-09` progress: bibliography lane도 계속 쪼개서 `smoke/bibliography.rs`는 host file이 되었고, `smoke/bibliography_multi.rs`도 `bibliography_multi_same_page`, `bibliography_multi_replay`, `bibliography_multi_rebuild`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/bibliography_checkpoint_preference.rs`도 include-only host가 되었고, checkpoint-preference matrix는 `bibliography_checkpoint_preference_included_body`, `bibliography_checkpoint_preference_dirty_only`, `bibliography_checkpoint_preference_toplevel`로 다시 나뉘었다. representative exact checks와 earlier-input / dirty-only / earlier-toplevel filters는 green이다
  - `2026-04-14` progress: `smoke/bibliography_checkpoint_preference_toplevel.rs`와 `smoke/bibliography_checkpoint_preference_included_body.rs`도 host file이 되었고, earlier-checkpoint preference matrix는 각각 `..._reversed`, `..._untracked`, `..._unreadable`로 다시 나뉘었다. targeted exact checkpoint-preference lanes `12/12`는 green이다
  - `2026-04-10` progress: `smoke/bibliography_semantically_equal_replay.rs`도 include-only host가 되었고, semantically-equal bibliography replay matrix는 `bibliography_semantically_equal_replay_included_body`, `bibliography_semantically_equal_replay_later_trailing_input`, `bibliography_semantically_equal_replay_later_trailing_input_toplevel`로 다시 나뉘었다. representative exact checks와 included-body / later-trailing-input filters는 green이다
  - `2026-04-14` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input.rs`도 include-only host가 되었고, later-trailing-input replay/fallback matrix는 `..._reversed`, `..._untracked`, `..._unreadable`로 다시 나뉘었다. targeted exact later-trailing-input lanes `6/6`은 green이다
  - `2026-04-15` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_toplevel.rs`도 include-only host가 되었고, top-level-document later-trailing-input replay/fallback matrix는 `..._reversed`, `..._untracked`, `..._unreadable`로 다시 나뉘었다. targeted exact later-trailing-input toplevel lanes `6/6`은 green이다
  - `2026-04-09` progress: `smoke/bibliography_multi_same_page.rs`도 host file이 되었고, same-page matrix는 `bibliography_multi_same_page_checkpoint_preference`, `bibliography_multi_same_page_trailing_input`, `bibliography_multi_same_page_trailing_input_included_body`, `bibliography_multi_same_page_late_change`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_replay.rs`도 host file이 되었고, cross-page replay matrix는 `bibliography_multi_replay_checkpoint_preference` + `bibliography_multi_replay_trailing_input`으로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/bibliography_multi_replay_checkpoint_preference.rs`도 include-only host가 되었고, cross-page multi-bibliography checkpoint-preference matrix는 `..._page_ordering`, `..._toplevel_baseline`, `..._toplevel_dirty`, `..._included_body_baseline`, `..._included_body_dirty`로 다시 나뉘었다. representative exact checks와 `semantically_equal_multi_bibliography` / `earlier_bibliography_page` filters는 green이다
  - `2026-04-10` progress: `smoke/bibliography_multi_replay_trailing_input.rs`도 include-only host가 되었고, cross-page multi-bibliography trailing-input replay matrix는 `..._included_tail_baseline`, `..._toplevel_baseline`, `..._toplevel_dirty`, `..._included_tail_dirty`로 다시 나뉘었다. representative exact checks와 trailing-input filters는 green이다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild.rs`도 host file이 되었고, 그 안의 matrix는 `bibliography_multi_rebuild_trailing_input`, `bibliography_multi_rebuild_late_bibliography`, `bibliography_multi_rebuild_rev5`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5.rs`도 host file이 되었고, 그 안의 fixture-backed matrix는 `bibliography_multi_rebuild_rev5_toplevel`, `bibliography_multi_rebuild_rev5_input`, `bibliography_multi_rebuild_rev5_late_change`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_toplevel.rs`는 `bibliography_multi_rebuild_rev5_baseline` + `bibliography_multi_rebuild_rev5_tracked_toplevel`로, `smoke/bibliography_multi_rebuild_rev5_input.rs`는 `bibliography_multi_rebuild_rev5_input_baseline` + `bibliography_multi_rebuild_rev5_input_sibling`으로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling.rs`도 `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_ordering` + `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_ordering.rs`도 host file이 되었고, tracked-toplevel sibling matrix는 `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_ordering_baseline` + `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_ordering_extra_dirty` + `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_ordering_interleaved`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/bibliography_multi_same_page_checkpoint_preference.rs`도 include-only host가 되었고, same-page multi-bibliography checkpoint-preference matrix는 `..._page_ordering`, `..._toplevel_baseline`, `..._included_body_baseline`, `..._included_body_dirty`, `..._toplevel_reversed_baseline`, `..._toplevel_dirty`로 다시 나뉘었다. representative exact checks와 same-page multi-bibliography filters는 green이다
  - `2026-04-09` progress: `smoke/bibliography_multi_same_page_late_change.rs`도 host file이 되었고, same-page semantic-change matrix는 `bibliography_multi_same_page_late_change_root` + `bibliography_multi_same_page_late_change_included_body`로 갈려 top-level vs included-body topology를 분리하게 됐다
  - `2026-04-10` progress: `smoke/bibliography_multi_same_page_late_change_root.rs`도 include-only host가 되었고, root late semantic-change matrix는 `..._baseline`, `..._sibling_dirty_orders`, `..._sibling_interleaved`, `..._tail_noise`로 다시 나뉘었다. representative exact checks와 combined `late_semantic_same_page_multi_bibliography_change_with_later_trailing_input_change` filter는 `44/44` green이다
  - `2026-04-29` progress: cross-page와 same-page multi-bibliography checkpoint page-ordering leaves가 이제 `tests/support/bibliography_multi_checkpoint_preference_page_ordering_helpers.rs`의 rev2 compile/dirty-file order helper를 공유한다. cross-page page-ordering leaf는 `20`줄, same-page page-ordering leaf는 `45`줄, helper는 `231`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-10` progress: `smoke/bibliography_multi_same_page_late_change_included_body.rs`도 include-only host가 되었고, included-body late semantic-change matrix는 `..._baseline`, `..._sibling_dirty_orders`, `..._sibling_interleaved`, `..._tail_noise`로 다시 나뉘었다. representative exact checks와 `later_trailing_input_change_with_included_body` family filter는 `66/66` green이다
  - `2026-04-09` progress: `smoke/bibliography_multi_same_page_trailing_input.rs`와 `smoke/bibliography_multi_same_page_trailing_input_included_body.rs`도 host file이 되었고, replay-side same-page matrix는 각각 `..._baseline` + `..._sibling`으로 한 단계 더 잘려 top-level / included-body 양쪽 모두에서 baseline vs unchanged-sibling `.bbl` branch가 분리됐다
  - `2026-04-09` progress: top-level / included-body `smoke/bibliography_multi_same_page_trailing_input*_sibling.rs`도 host file이 되었고, sibling replay matrix는 각각 `..._sibling_ordering` + `..._sibling_extra_dirty`로 더 잘렸다
  - `2026-04-09` progress: top-level / included-body `smoke/bibliography_multi_same_page_trailing_input*_sibling_ordering.rs`도 host file이 되었고, ordering-only replay cases는 각각 `..._sibling_ordering_baseline` + `..._sibling_ordering_interleaved`로 다시 잘렸다
  - `2026-04-09` progress: top-level / included-body `smoke/bibliography_multi_same_page_trailing_input*_sibling_extra_dirty.rs`도 host file이 되었고, extra-dirty replay/fallback cases는 각각 `..._sibling_extra_dirty_untracked` + `..._sibling_extra_dirty_unreadable`로 다시 잘렸다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input.rs`도 host file이 되었고, late semantic multi-bibliography trailing-input rebuild matrix는 `bibliography_multi_rebuild_trailing_input_root` + `bibliography_multi_rebuild_trailing_input_included_body`로 갈려 top-level vs included-body topology가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_root.rs`도 host file이 되었고, top-level trailing-input rebuild cases는 `bibliography_multi_rebuild_trailing_input_root_baseline` + `bibliography_multi_rebuild_trailing_input_root_extra_dirty`로 다시 나뉘어 plain/reversed baseline과 readable/unreadable noise quartet이 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_root_extra_dirty.rs`도 host file이 되었고, top-level trailing-input no-sibling noise quartet은 `bibliography_multi_rebuild_trailing_input_root_extra_dirty_untracked` + `bibliography_multi_rebuild_trailing_input_root_extra_dirty_unreadable`로 다시 나뉘어 readable-noise rebuild와 unreadable-noise rebuild가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body.rs`도 host file이 되었고, included-body trailing-input rebuild matrix는 `bibliography_multi_rebuild_trailing_input_included_body_baseline` + `bibliography_multi_rebuild_trailing_input_included_body_sibling` + `bibliography_multi_rebuild_trailing_input_included_body_extra_dirty`로 다시 나뉘어 baseline, unchanged-sibling `.bbl`, no-sibling noise branch가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_extra_dirty.rs`도 host file이 되었고, included-body trailing-input no-sibling noise quartet은 `bibliography_multi_rebuild_trailing_input_included_body_extra_dirty_untracked` + `bibliography_multi_rebuild_trailing_input_included_body_extra_dirty_unreadable`로 다시 나뉘어 readable-noise rebuild와 unreadable-noise rebuild가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography.rs`도 host file이 되었고, late-bibliography matrix는 `bibliography_multi_rebuild_late_bibliography_replay_and_skip` + `bibliography_multi_rebuild_late_bibliography_rebuild`로 다시 나뉘어 semantically-equal replay/skip/fallback family와 conservative rebuild family가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_replay_and_skip.rs`도 host file이 되었고, 그 안의 non-rebuild contracts는 `bibliography_multi_rebuild_late_bibliography_replay_included_body` + `bibliography_multi_rebuild_late_bibliography_skip_and_rewrite`로 다시 나뉘어 included-body replay/fallback matrix와 skip/cp0/rewrite-span cases가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body.rs`도 host file이 되었고, included-body replay/fallback matrix는 `bibliography_multi_rebuild_late_bibliography_replay_included_body_baseline` + `bibliography_multi_rebuild_late_bibliography_replay_included_body_extra_dirty`로 다시 나뉘어 clean/reversed replay와 extra-dirty fallback quartet이 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_skip_and_rewrite.rs`도 host file이 되었고, late-bibliography non-rebuild contracts는 `bibliography_multi_rebuild_late_bibliography_skip_cases` + `bibliography_multi_rebuild_late_bibliography_rewrite_cases`로 다시 나뉘어 skip/reuse regressions와 cp0/rewrite-span regressions가 분리됐다
  - `2026-04-09` progress: `smoke/replay_selection.rs`도 host file이 되었고, broad replay-selector matrix는 `replay_selection_late_toplevel` + `replay_selection_late_input` + `replay_selection_input_boundaries` + `replay_selection_include_boundaries`로 다시 나뉘어 late-toplevel fallback, late-input removal/replay, input-boundary preference, include-boundary preference family가 분리됐다
  - `2026-04-09` progress: `smoke/replay_selection_late_input.rs`도 host file이 되었고, late-input family는 `replay_selection_late_input_page_replay` + `replay_selection_late_input_removal`으로 다시 나뉘어 single late-input page replay regression과 late-input-removal rebuild matrix가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_input_boundaries.rs`도 host file이 되었고, input-boundary family는 `replay_selection_input_boundaries_late_input_noise` + `replay_selection_input_boundaries_same_page` + `replay_selection_input_boundaries_earlier_page` + `replay_selection_input_boundaries_toplevel_exit`로 다시 나뉘어 late-input noise fallback, same-page checkpoint preference, earlier-page checkpoint preference, top-level input-exit replay가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_input_boundaries_same_page.rs`와 `smoke/replay_selection_input_boundaries_earlier_page.rs`도 host file이 되었고, same-page branch는 `..._same_page_cp0` + `..._same_page_multi_file` + `..._same_page_toplevel` + `..._same_page_toplevel_appendix`, earlier-page branch는 `..._earlier_page_baseline` + `..._earlier_page_toplevel` + `..._earlier_page_toplevel_appendix`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/replay_selection_late_input_removal.rs`도 pure host file이 되었고, late-input-removal matrix는 `replay_selection_late_input_removal_deleted` + `replay_selection_late_input_removal_retained_dirty` + `replay_selection_late_input_removal_file_remains`로 다시 나뉘었다. `late_input` family는 계속 `21/21` green이다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries.rs`도 host file이 되었고, include-boundary family는 `replay_selection_include_boundaries_repeated_include` + `replay_selection_include_boundaries_nested_exit`로 다시 나뉘어 repeated-include occurrence preference와 nested input-exit preference가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries_repeated_include.rs`도 host file이 되었고, repeated-include branch는 `replay_selection_include_boundaries_repeated_include_baseline` + `replay_selection_include_boundaries_repeated_include_toplevel`로 다시 나뉘어 baseline/multi-file cases와 late-toplevel competition cases가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries_nested_exit.rs`도 host file이 되었고, nested-exit branch는 `replay_selection_include_boundaries_nested_exit_core` + `replay_selection_include_boundaries_nested_exit_multi_file`로 다시 나뉘어 core baseline/late-toplevel matrix와 multi-file case가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_late_toplevel.rs`도 host file이 되었고, late-toplevel family는 `replay_selection_late_toplevel_shipout` + `replay_selection_late_toplevel_noise`로 다시 나뉘어 pure shipout-checkpoint replay regression과 noisy preamble-fallback/replay matrix가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries_nested_exit_core.rs`도 host file이 되었고, nested-exit core branch는 `replay_selection_include_boundaries_nested_exit_core_baseline` + `replay_selection_include_boundaries_nested_exit_core_toplevel`로 다시 나뉘어 pure nested-exit baseline과 late-toplevel competition matrix가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_late_toplevel_noise.rs`도 host file이 되었고, late-toplevel noisy fallback/replay cases는 `replay_selection_late_toplevel_noise_untracked` + `replay_selection_late_toplevel_noise_unreadable`로 다시 나뉘어 readable-noise와 unreadable-noise leaf가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries_repeated_include_toplevel.rs`도 host file이 되었고, repeated-include late-toplevel competition cases는 `replay_selection_include_boundaries_repeated_include_toplevel_plain` + `replay_selection_include_boundaries_repeated_include_toplevel_appendix`로 다시 나뉘어 plain late-toplevel competition과 appendix-competition matrix가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_late_toplevel_noise_untracked.rs`와 `smoke/replay_selection_late_toplevel_noise_unreadable.rs`도 host file이 되었고, 각각 `..._follows` + `..._precedes`로 다시 나뉘어 dirty-file order variant가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_input_boundaries_late_input_noise.rs`도 host file이 되었고, late-input noise fallback cases는 `replay_selection_input_boundaries_late_input_noise_untracked` + `replay_selection_input_boundaries_late_input_noise_unreadable`로 다시 나뉘어 readable-noise와 unreadable-noise leaf가 분리됐다
  - `2026-04-10` progress: `smoke/replay_selection_input_boundaries_late_input_noise_untracked.rs`와 `smoke/replay_selection_input_boundaries_late_input_noise_unreadable.rs`도 host file이 되었고, 각각 `..._follows` + `..._precedes`로 다시 나뉘어 late-input noise dirty-order pair가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild.rs`도 host file이 되었고, conservative rebuild matrix는 `bibliography_multi_rebuild_late_bibliography_rebuild_toplevel` + `bibliography_multi_rebuild_late_bibliography_rebuild_included_file`로 다시 나뉘어 top-level late-bibliography-only rebuild quartet과 included-file late-change rebuild family가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_toplevel.rs`도 host file이 되었고, top-level late-bibliography-only rebuild quartet은 `bibliography_multi_rebuild_late_bibliography_rebuild_toplevel_untracked` + `bibliography_multi_rebuild_late_bibliography_rebuild_toplevel_unreadable`로 다시 나뉘어 readable-noise rebuild와 unreadable-noise rebuild가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file.rs`도 host file이 되었고, included-file rebuild family는 `bibliography_multi_rebuild_late_bibliography_rebuild_included_file_baseline` + `bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked`로 다시 나뉘어 no-later-change noise quartet과 later-tracked-change quartet이 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked.rs`도 host file이 되었고, later-tracked-change rebuild family는 `bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_baseline` + `bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_extra_dirty`로 다시 나뉘어 plain later-tracked baseline과 readable/unreadable extra-dirty quartet이 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_extra_dirty.rs`도 host file이 되었고, later-tracked extra-dirty quartet은 `bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_extra_dirty_untracked` + `bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_extra_dirty_unreadable`로 다시 나뉘어 readable-noise rebuild와 unreadable-noise rebuild가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling.rs`도 host file이 되었고, unchanged-sibling `.bbl` branch는 `bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering` + `bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty`로 다시 나뉘어 ordering-only branch와 extra-dirty fallback branch가 분리됐다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering.rs`도 host file이 되었고, ordering-only cases는 `bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering_baseline` + `bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering_interleaved`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty.rs`도 host file이 되었고, extra-dirty sibling cases는 `bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_untracked` + `bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_late_change.rs`도 host file이 되었고, 그 안의 late semantic-change matrix는 `bibliography_multi_rebuild_rev5_late_change_root` + `bibliography_multi_rebuild_rev5_late_change_included_body`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_root.rs`도 host file이 되었고, plain late-change baseline/helpers와 extra-dirty quartet은 `bibliography_multi_rebuild_rev5_late_change_root_baseline` + `bibliography_multi_rebuild_rev5_late_change_root_extra_dirty`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_included_body.rs`도 host file이 되었고, included-body baseline과 extra-dirty quartet은 `bibliography_multi_rebuild_rev5_late_change_included_body_baseline` + `bibliography_multi_rebuild_rev5_late_change_included_body_extra_dirty`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_ordering.rs`도 host file이 되었고, tracked-input sibling ordering matrix는 `bibliography_multi_rebuild_rev5_input_sibling_ordering_baseline` + `bibliography_multi_rebuild_rev5_input_sibling_ordering_extra_dirty`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_extra_dirty.rs`도 host file이 되었고, 그 안의 noise matrix는 `bibliography_multi_rebuild_rev5_input_sibling_extra_dirty_interleaved` + `bibliography_multi_rebuild_rev5_input_sibling_extra_dirty_other_interleaved`로 다시 나뉘었다. 이 과정에서 shared tracked-tail-input fixture/assert helper는 `bibliography_multi_rebuild_rev5_tracked_tail_input_helpers.rs`로 공용화했다
  - `2026-04-25` progress: rev5 tracked-tail-input helper는 이제 smoke 디렉터리가 아니라 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_tracked_tail_input_helpers.rs`에 있다. `smoke/bibliography_multi_rebuild_rev5.rs`는 support include만 참조하며, `tests/smoke`에 남아 있던 90줄 이상 helper 파일은 제거됐다. later-tracked-input fixture-branch filter `22/22`는 green이다
  - `2026-04-09` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty.rs`도 host file이 되었고, tracked-toplevel extra-dirty quartet은 `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty_interleaved` + `bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty_other_interleaved`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/unchanged_tail.rs`도 host file이 되었고, `unchanged_tail_noop` lane은 `unchanged_tail_noop_toplevel`, `unchanged_tail_noop_single_source`, `unchanged_tail_noop_ordering`, `unchanged_tail_noop_multi_input`, `unchanged_tail_noop_with_toplevel`, `unchanged_tail_noop_toplevel_permutations`까지 나뉘었다
  - `2026-04-14` progress: `smoke/unchanged_tail_mutations.rs`도 include-only host가 되었고, unchanged-tail mutation matrix는 `..._shifted_and_deleted`, `..._inserted_and_replaced`로 다시 나뉘었다. targeted exact mutation lanes `4/4`는 green이다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_ordering.rs`도 include-only host가 되었고, ordering-sensitive unchanged-tail noop matrix는 `..._mixed_leading`, `..._double_leading_dirty_order`, `..._double_leading_other_interleaved`, `..._multi_file_dirty_order`로 다시 나뉘었다. representative exact checks와 mixed-input / mixed-toplevel / double-leading / multi-file filters는 green이다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_multi_input.rs`도 include-only host가 되었고, multi-input unchanged-tail noop matrix는 `..._dirty_orders`, `..._between_noise`, `..._leading_dirty_orders`, `..._leading_between_noise`, `..._mixed_dirty_orders`, `..._mixed_between_noise`로 다시 나뉘었다. representative exact checks와 `nonrendering_multi_input` / `nonrendering_mixed_multi_input` filters가 green이다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_toplevel_permutations.rs`도 host file이 되었고, 그 안의 matrix는 `unchanged_tail_noop_mixed_toplevel`, `unchanged_tail_noop_leading_toplevel`, `unchanged_tail_noop_trailing_toplevel`, `unchanged_tail_noop_leading_toplevel_variants`, `unchanged_tail_noop_tail_cleanup`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_mixed_toplevel.rs`도 include-only host가 되었고, mixed top-level + multi-input unchanged-tail noop matrix는 `..._dirty_orders`, `..._interleaved_orders`, `..._other_interleaved_orders`로 다시 나뉘었다. representative exact checks와 `nonrendering_mixed_toplevel_and_multi_input_edits` filter는 `16/16` green이다
  - `2026-04-14` progress: `smoke/unchanged_tail_noop_mixed_toplevel_dirty_orders.rs`도 include-only host가 되었고, mixed-top-level dirty-order noop matrix는 `..._baseline_and_reversed`, `..._reversed_noise_follows`, `..._noise_follows`로 다시 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_leading_toplevel.rs`도 include-only host가 되었고, leading-top-level + multi-input matrix는 `..._dirty_order_baseline`, `..._dirty_order_follows`, `..._dirty_order_precedes`, `..._interleaved_orders`, `..._other_interleaved_orders`로 다시 나뉘었다. representative exact checks와 `nonrendering_leading_toplevel_and_multi_input_edits` filter는 `20/20` green이다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_trailing_toplevel.rs`도 include-only host가 되었고, trailing-top-level + multi-input matrix는 `..._multi_input_leading_dirty_orders`, `..._mixed_multi_input_dirty_orders`, `..._multi_input_leading_interleaved_orders`, `..._multi_input_leading_other_interleaved_orders`, `..._mixed_multi_input_interleaved_orders`, `..._mixed_multi_input_other_interleaved_orders`로 다시 나뉘었다. representative exact checks와 `nonrendering_trailing_toplevel_and` filter는 `24/24` green이다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_tail_cleanup.rs`도 host file이 되었고, 그 안의 matrix는 `unchanged_tail_noop_tail_cleanup_multi_file_other_interleaved`, `unchanged_tail_noop_tail_cleanup_double_leading`, `unchanged_tail_noop_tail_cleanup_multi_file`, `unchanged_tail_noop_tail_cleanup_only_dirty`로 다시 나뉘었다
  - `2026-04-14` progress: `smoke/unchanged_tail_noop_tail_cleanup_multi_file.rs`도 include-only host가 되었고, multi-file tail-cleanup noop matrix는 `..._base_and_follows`, `..._precedes`, `..._interleaved_between`로 다시 나뉘었다. targeted exact checks `7/7`은 green이다
  - `2026-04-14` progress: `smoke/unchanged_tail_noop_tail_cleanup_double_leading.rs`도 include-only host가 되었고, double-leading tail-cleanup noop matrix는 `..._base_and_follows`, `..._precedes`, `..._interleaved_between`로 다시 나뉘었다. targeted exact checks `7/7`은 green이다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants.rs`도 include-only host가 되었고, leading-toplevel multi-input matrix는 `..._multi_input_dirty_orders`, `..._multi_input_interleaved_orders`, `..._mixed_multi_input_dirty_orders`, `..._mixed_multi_input_interleaved_orders`로 다시 나뉘었다. representative exact checks와 `nonrendering_leading_toplevel_and` family filter는 `60/60` green이다
  - `2026-04-10` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders.rs`도 host file이 되었고, multi-input leading-edit interleaved dirty-order matrix는 `..._primary` + `..._other`로 다시 나뉘어 primary interleaved-order branch와 other-interleaved-order branch가 분리됐다. targeted filters는 green이다
  - `2026-04-11` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders.rs`도 host file이 되었고, mixed-multi-input dirty-order matrix는 `..._plain` + `..._reversed`로 다시 나뉘어 plain dirty-order branch와 reversed dirty-order branch가 분리됐다. targeted exact/plain/reversed checks는 green이다
  - `2026-04-11` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders.rs`도 host file이 되었고, mixed-multi-input interleaved dirty-order matrix는 `..._primary` + `..._other`로 다시 나뉘어 primary interleaved-order branch와 other-interleaved-order branch가 분리됐다. targeted filters는 green이다
  - `2026-04-11` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders.rs`도 host file이 되었고, multi-input leading-edit dirty-order matrix는 `..._baseline_follows` + `..._precedes`로 다시 나뉘어 baseline/follows branch와 precedes branch가 분리됐다. targeted exact/reversed/and_-family checks는 green이다
  - `2026-04-14` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_baseline_follows.rs`도 include-only host가 되었고, baseline/follows branch는 `..._baseline_and_reversed`, `..._reversed_noise_follows`, `..._noise_follows`로 다시 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-11` progress: `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders.rs`도 host file이 되었고, mixed-multi-input dirty-order matrix는 `..._baseline_follows` + `..._precedes`로 다시 나뉘어 baseline/follows branch와 precedes branch가 분리됐다. targeted exact/reversed/and_-family checks는 green이다
  - `2026-04-11` progress: `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders.rs`도 host file이 되었고, nonrendering multi-input leading-edit dirty-order matrix는 `..._baseline_follows` + `..._precedes`로 다시 나뉘어 baseline/follows branch와 precedes branch가 분리됐다. targeted exact/reversed/and_-family checks는 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_baseline_follows.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows.rs`도 include-only host가 되었고, 세 baseline/follows leaf는 모두 `..._baseline_and_reversed`, `..._reversed_noise_follows`, `..._noise_follows`로 다시 나뉘었다. targeted exact lanes `18/18`은 green이다
  - `2026-04-12` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable.rs`도 host file이 되었고, late-semantic multi-bibliography rebuild matrix는 `..._interleaved_orders` + `..._plain_dirty_orders`로 다시 나뉘어 interleaved branch와 plain dirty-order branch가 분리됐다. targeted bibliography rebuild filters는 green이다
  - `2026-04-14` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_interleaved_orders.rs`도 include-only host가 되었고, 남은 interleaved-order rebuild matrix는 `..._other_interleaved_unreadable`, `..._interleaved_untracked`, `..._interleaved_unreadable`로 다시 나뉘었다. targeted exact rebuild lanes `6/6`은 green이다
  - `2026-04-13` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved.rs`도 host file이 되었고, same-page late-semantic included-body bibliography matrix는 `..._base_and_interleaved_orders` + `..._other_interleaved_variants`로 다시 나뉘어 base/interleaved branch와 other-interleaved branch가 분리됐다. targeted included-body interleaved filters는 green이다
  - `2026-04-15` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders.rs`와 `smoke/bibliography_multi_same_page_late_change_root_sibling_dirty_orders.rs`도 include-only host가 되었고, 남은 dirty-order branch는 `..._baseline_and_reversed`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact rebuild lanes `12/12`는 green이다
  - `2026-04-15` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_base_and_interleaved_orders.rs`와 `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_base_and_interleaved_orders.rs`도 include-only host가 되었고, 남은 interleaved-order branch는 `..._interleaved_variants`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact rebuild lanes `12/12`는 green이다
  - `2026-04-15` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_unreadable.rs`도 include-only host가 되었고, top-level same-page trailing-input extra-dirty unreadable-noise matrix는 `..._baseline_unreadable`, `..._interleaved_unreadable`, `..._other_interleaved_unreadable`로 다시 나뉘었다. targeted replay/fallback exact lanes `6/6`은 green이다
  - `2026-04-15` progress: `smoke/bibliography_fixpoint_reuse.rs`도 include-only host가 되었고, semantic fixpoint-reuse matrix는 `..._toplevel` + `..._included_body`로 다시 나뉘었다. targeted exact fixpoint/unchanged-tail lanes `6/6`은 green이다
  - `2026-04-15` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling.rs`도 include-only host가 되었고, rev5 multi-bibliography rebuild matrix는 `..._dirty_order_variants`, `..._interleaved_variants`, `..._other_interleaved_variants`로 다시 나뉘었다. targeted exact rebuild lanes `16/16`은 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_other_interleaved_orders.rs`와 `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_interleaved_orders.rs`도 include-only host가 되었고, trailing-toplevel interleaved unchanged-tail matrix는 각각 `..._base`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact unchanged-tail lanes `10/10`은 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_reversed.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_plain.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_other.rs`, `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_interleaved_orders.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_primary.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_primary.rs`, `smoke/unchanged_tail_noop_leading_toplevel_interleaved_orders.rs`도 include-only host가 되었고, 남은 interleaved/reversed/plain unchanged-tail matrix는 모두 `..._base`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact unchanged-tail lanes `35/35`는 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_other_interleaved_orders.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_other.rs`, `smoke/unchanged_tail_noop_mixed_toplevel_interleaved_orders.rs`, `smoke/unchanged_tail_noop_mixed_toplevel_other_interleaved_orders.rs`, `smoke/unchanged_tail_noop_leading_toplevel_other_interleaved_orders.rs`도 include-only host가 되었고, 남은 other-interleaved/interleaved unchanged-tail matrix는 모두 `..._base`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact unchanged-tail lanes `25/25`는 green이다
  - `2026-04-15` progress: `smoke/bibliography_features_biblatex_print.rs`, `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order.rs`, `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order.rs`도 include-only host가 되었고, biblatex print matrix는 `..._core_and_smartcite`, `..._headings_and_supercite`, `..._entries_and_multicite`로, ordering unchanged-tail matrix 둘은 각각 `..._base`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted smoke lanes `19/19`는 green이다
  - `2026-04-15` progress: `smoke/bibliography_multi_rebuild_rev5_baseline.rs`도 include-only host가 되었고, rev5 baseline rebuild matrix는 `..._core`, `..._support`, `..._noise`, `..._sibling_dirty`로 다시 나뉘었다. 또 `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body_extra_dirty.rs`와 `smoke/bibliography_multi_replay_checkpoint_preference_included_body_dirty.rs`도 include-only host가 되었고, 두 included-body dirty-order matrix는 각각 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `16/16`은 green이다
  - `2026-04-15` progress: `smoke/bibliography_semantic_features.rs`, `smoke/bibliography_multi_replay_trailing_input_toplevel_dirty.rs`, `smoke/bibliography_multi_replay_trailing_input_included_tail_dirty.rs`도 include-only host가 되었고, semantic-features matrix는 `..._output_and_include`, `..._semantic_index`, `..._backdate_and_rebuild`로, trailing-input dirty-order matrix 둘은 각각 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `16/16`은 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_single_source_trailing_input.rs`, `smoke/unchanged_tail_noop_toplevel.rs`, `smoke/bibliography_multi_replay_checkpoint_preference_toplevel_dirty.rs`도 include-only host가 되었고, 두 unchanged-tail matrix는 각각 `..._base`, `..._untracked_noise`, `..._unreadable_noise`로, checkpoint-preference toplevel dirty-order matrix는 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `14/14`은 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_reversed.rs`, `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_plain.rs`, `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_baseline.rs`도 include-only host가 되었고, 두 unchanged-tail trailing-noise matrix는 각각 `..._untracked_noise`, `..._unreadable_noise`로, included-file late-bibliography baseline rebuild matrix는 `..._base`, `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `13/13`은 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_noise_reversed.rs`, `smoke/unchanged_tail_noop_with_toplevel_leading_noise_plain.rs`, `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_precedes.rs`도 include-only host가 되었고, 세 unchanged-tail leading-noise/dirty-order matrix는 모두 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `12/12`은 green이다
  - `2026-04-15` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_precedes.rs`, `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_follows.rs`, `smoke/bibliography_multi_same_page_checkpoint_preference_included_body_dirty.rs`도 include-only host가 되었고, 두 unchanged-tail matrix와 same-page checkpoint-preference included-body dirty-order matrix는 모두 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `12/12`은 green이다
  - `2026-04-15` progress: `smoke/bibliography_semantically_equal_replay_included_body.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_plain_dirty_orders.rs`, `smoke/bibliography_multi_same_page_checkpoint_preference_toplevel_dirty.rs`도 include-only host가 되었고, 세 bibliography replay/rebuild matrix는 모두 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `12/12`은 green이다
  - `2026-04-16` progress: `smoke/unchanged_tail_noop_multi_input_mixed_between_noise.rs`, `smoke/unchanged_tail_noop_multi_input_leading_between_noise.rs`, `smoke/unchanged_tail_noop_multi_input_between_noise.rs`도 include-only host가 되었고, 세 unchanged-tail interleaved/other-interleaved between-noise matrix는 모두 `..._untracked_noise`, `..._unreadable_noise`로 다시 나뉘었다. targeted exact lanes `12/12`은 green이다
  - `2026-04-16` progress: `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_precedes.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes.rs`, `smoke/external_oracle_internal_compiler.rs`도 include-only host가 되었다. 앞의 두 unchanged-tail dirty-order-precedes matrix는 `..._untracked_noise`, `..._unreadable_noise` leaf로 쪼갰고, internal-compiler oracle smoke는 `external_oracle_internal_compiler_core.rs`와 `external_oracle_internal_compiler_compat.rs`로 분리했다. targeted exact는 `15/16` green이고, 남은 `bundled_arxiv_basic_fixture_builds_with_internal_compiler`는 기존 dirty `fixtures/arxiv-basic/main.tex` 때문에 현재 workspace에서만 깨진다
  - `2026-04-16` progress: `smoke/bibliography_checkpoint_preference_dirty_only.rs`, `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_other_interleaved_variants.rs`, `smoke/external_oracle_pipeline_success_pdf_latex.rs`, `smoke/external_oracle_pipeline_success_xelatex_and_meta.rs`도 include-only host가 되었다. 두 bibliography family는 `..._untracked_noise`, `..._unreadable_noise` leaf로 나뉘었고, pdf-latex success family는 `..._tectonic.rs`와 `..._pdflatex.rs`로, xelatex/meta success family는 `..._meta.rs`와 `..._recorder.rs`로 분리했다. targeted exact lanes `20/20`은 green이다
  - `2026-04-16` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_other_interleaved_variants.rs`, `smoke/unchanged_tail_noop_ordering_mixed_leading.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_baseline_baseline_reversed_and_follows.rs`도 include-only host가 되었다. root-sibling interleaved bibliography family는 `..._untracked_noise`, `..._unreadable_noise`로, mixed-leading unchanged-tail ordering family는 `..._baseline.rs`, `..._reversed.rs`로, trailing-input included-body baseline family는 `..._baseline_and_reversed.rs`, `..._untracked_follows.rs`로 나뉘었다. targeted exact lanes `11/11`은 green이다
  - `2026-04-16` progress: `smoke/unchanged_tail_mutations_shifted_and_deleted.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_baseline_precedes_and_unreadable.rs`, `smoke/reference_variants_theorem_setup.rs`도 include-only host가 되었다. unchanged-tail mutations file은 `..._shifted.rs`, `..._deleted.rs`로, trailing-input included-body bibliography family는 `..._untracked_precedes.rs`, `..._unreadable.rs`로, theorem setup reference variants는 `..._kinds.rs`, `..._newtheorem.rs`, `..._presentation.rs`로 나뉘었다. targeted exact lanes `13/13`은 green이다
  - `2026-04-16` progress: `smoke/unchanged_tail_mutations_inserted_and_replaced.rs`, `smoke/bibliography_multi_replay_checkpoint_preference_toplevel_baseline.rs`, `smoke/bibliography_features_citation_fields_identifiers.rs`, `smoke/bibliography_multi_same_page_late_change_included_body_baseline.rs`도 include-only host가 되었다. unchanged-tail mutations file은 `..._inserted.rs`, `..._replaced.rs`로, toplevel replay-checkpoint baseline family는 `..._baseline.rs`, `..._reversed.rs`로, citation-field identifiers family는 `..._url_and_number.rs`, `..._identifier_and_date.rs`로, included-body late-change baseline family는 `..._baseline_and_reversed.rs`, `..._untracked_follows.rs`로 나뉘었다. targeted exact lanes `13/13`은 green이다
  - `2026-04-16` progress: `smoke/bibliography_multi_same_page_late_change_included_body_tail_noise.rs`, `smoke/bibliography_multi_same_page_late_change_root_baseline.rs`, `smoke/unchanged_tail_noop_tail_cleanup_multi_file_base_and_follows.rs`, `smoke/unchanged_tail_noop_tail_cleanup_double_leading_base_and_follows.rs`도 include-only host가 되었다. included-body tail-noise family는 `..._untracked_precedes.rs`, `..._unreadable.rs`로, root late-change baseline family는 `..._baseline_and_reversed.rs`, `..._untracked_follows.rs`로, 두 unchanged-tail tail-cleanup family는 모두 `..._baseline_and_untracked.rs`, `..._unreadable.rs`로 나뉘었다. targeted exact lanes `12/12`은 green이다
  - `2026-04-16` progress: `smoke/bibliography_multi_same_page_late_change_root_tail_noise.rs`, `smoke/bibliography_fixpoint_reuse_included_body.rs`, `smoke/bibliography_features_natbib_variants.rs`, `smoke/bibliography_multi_replay_checkpoint_preference_included_body_baseline.rs`도 include-only host가 되었다. root tail-noise family는 `..._untracked_precedes.rs`, `..._unreadable.rs`로, included-body fixpoint-reuse family는 `..._replay_checkpoint.rs`, `..._tail_noise.rs`로, natbib variants family는 `..._textual.rs`, `..._other_variants.rs`로, included-body replay-checkpoint baseline family는 `..._baseline.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `14/14`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_preview_and_warnings.rs`와 `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body_baseline.rs`도 include-only host가 되었다. custom-compiler preview/warning family는 `..._preview_and_stdout.rs`, `..._placeholders_and_stderr.rs`로, included-body late-bibliography replay baseline family는 `..._baseline.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_preview_and_warnings_preview_and_stdout.rs`도 include-only host가 되었다. preview/stdout-warning coverage는 `..._preview.rs`, `..._stdout.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_preview_and_warnings_placeholders_and_stderr.rs`도 include-only host가 되었다. placeholder/stderr-warning coverage는 `..._placeholders.rs`, `..._stderr.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_trailing_input_included_tail_baseline.rs`와 `smoke/bibliography_multi_replay_trailing_input_toplevel_baseline.rs`도 include-only host가 되었다. 두 trailing-input replay baseline family는 모두 `..._baseline.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_missing_artifacts_output_files.rs`와 `smoke/bibliography_output_textstyle_and_starred.rs`도 include-only host가 되었다. missing-artifact oracle failure family는 `..._dvips_and_dvi.rs`, `..._ps2pdf_and_postscript.rs`, `..._pdf_outputs.rs`로, bibliography textstyle/starred wrapper family는 `..._case_and_urlstyle.rs`, `..._starred_wrappers.rs`, `..._name_affix.rs`로 나뉘었다. targeted exact lanes `11/11`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body_extra_dirty_untracked_noise.rs`와 `smoke/bibliography_multi_replay_checkpoint_preference_included_body_dirty_untracked_noise.rs`도 include-only host가 되었다. 두 untracked-noise bibliography replay leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_engine_failures_xelatex.rs`와 `smoke/bibliography_features_biblatex_print_core_and_smartcite.rs`도 include-only host가 되었다. xelatex failure family는 `..._exit_details.rs`, `..._spawn_and_warnings.rs`로, biblatex print core/smartcite family는 `..._core_and_preamble.rs`, `..._smartcite.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_engine_failures_xelatex_exit_details.rs`도 include-only host가 되었다. xelatex exit-detail coverage는 `..._stderr.rs`, `..._stdout.rs`, `..._quiet.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_engine_failures_tectonic.rs`도 include-only host가 되었다. tectonic exit-detail coverage는 `..._stderr.rs`, `..._stdout.rs`, `..._quiet.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_engine_failures_pdflatex.rs`도 include-only host가 되었다. pdflatex exit-detail coverage는 `..._stderr.rs`, `..._stdout.rs`, `..._quiet.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_checkpoint_preference_toplevel_reversed.rs`, `smoke/bibliography_multi_same_page_checkpoint_preference_included_body_baseline.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_baseline_baseline_reversed_and_follows_baseline_and_reversed.rs`도 include-only host가 되었다. 세 baseline/reversed bibliography replay leaf는 모두 `..._baseline.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_trailing_input_included_tail_dirty_untracked_noise.rs`, `smoke/bibliography_multi_replay_trailing_input_toplevel_dirty_untracked_noise.rs`, `smoke/bibliography_multi_replay_checkpoint_preference_toplevel_dirty_untracked_noise.rs`도 include-only host가 되었다. 세 dirty untracked-noise bibliography replay leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_dependency_tracking.rs`와 `smoke/reference_variants_basic_refs.rs`도 include-only host가 되었다. custom-compiler dependency-tracking family는 `..._fallback_and_depfile.rs`, `..._fls_and_unreadable.rs`로, basic reference variants는 `..._eq_and_subref.rs`, `..._name_and_title.rs`, `..._preferred_titles.rs`로 나뉘었다. targeted exact lanes `10/10`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_output_punctuation_and_spacing.rs`도 include-only host가 되었다. punctuation/spacing wrapper coverage는 `..._parentext_and_dash.rs`, `..._low_level_and_super_sub.rs`, `..._nolinkurl_path_detokenize.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_checkpoint_preference_included_body_dirty_unreadable_noise.rs`, `smoke/bibliography_multi_same_page_trailing_input_baseline_reversed.rs`, `smoke/bibliography_checkpoint_preference_included_body_reversed.rs`도 include-only host가 되었다. unreadable-noise replay leaf는 `..._follows.rs`, `..._precedes.rs`로, 두 baseline/reversed bibliography replay leaf는 `..._baseline.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_ordering_baseline.rs`와 `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_ordering_interleaved.rs`도 include-only host가 되었다. sibling-ordering baseline family는 `..._plain.rs`, `..._reversed.rs`로, interleaved family는 `..._primary.rs`, `..._other.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body_extra_dirty_unreadable_noise.rs`와 `smoke/bibliography_multi_replay_trailing_input_toplevel_dirty_unreadable_noise.rs`도 include-only host가 되었다. 두 unreadable-noise bibliography replay leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_output_basic_wrappers.rs`도 include-only host가 되었다. bibliography-output wrapper coverage는 `..._href_and_urlprefix.rs`, `..._common_and_newunit.rs`, `..._bibstring_and_acro.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_trailing_input_included_tail_dirty_unreadable_noise.rs`와 `smoke/bibliography_multi_same_page_trailing_input_sibling_ordering_baseline.rs`도 include-only host가 되었다. unreadable tail-dirty replay leaf는 `..._follows.rs`, `..._precedes.rs`로, same-page sibling-ordering baseline family는 `..._plain.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body.rs`는 이제 `support/bibliography_multi_rebuild_late_bibliography_replay_included_body_extra_dirty_helpers.rs`를 통해 rev1 fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline의 `..._baseline.rs` / `..._reversed.rs`와 extra-dirty의 untracked-noise / unreadable-noise `..._follows.rs` / `..._precedes.rs` leaf 여섯 개는 모두 dirty-file ordering만 남겼고, targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_trailing_input.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_replay_trailing_input_toplevel_helpers.rs`를 통해 toplevel trailing-input replay fixture, rev2 rewrite, replay/build-meta assert를 공유한다. toplevel baseline/reversed와 dirty untracked/unreadable leaf 여섯 개는 `159/159/138/161/138/138`줄에서 `25/25/27/27/27/27`줄로 줄었고, targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_trailing_input.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_replay_trailing_input_included_tail_helpers.rs`를 통해 included-tail trailing-input replay fixture, rev2 rewrite, replay/build-meta assert를 공유한다. included-tail baseline/reversed와 dirty untracked/unreadable leaf 여섯 개는 `159/159/138/161/138/138`줄에서 `26/26/28/28/28/28`줄로 줄었고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_checkpoint_preference.rs`는 이제 `crates/latexd/tests/support/bibliography_checkpoint_preference_toplevel_helpers.rs`를 통해 toplevel checkpoint-preference fixture, rev2 rewrite, replay/build-meta assert를 공유한다. reversed baseline/reversed leaf는 `19/19`줄, dirty untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf는 모두 `24`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_checkpoint_preference.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_checkpoint_preference_included_body_helpers.rs`를 통해 same-page included-body checkpoint-preference fixture, rev2 rewrite, replay/build-meta assert를 공유한다. included-body baseline/reversed leaf는 `23/23`줄, dirty untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf는 모두 `25`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_baseline.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_trailing_input_included_body_baseline_helpers.rs`를 통해 same-page included-body trailing-input replay fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/reversed leaf는 `22/22`줄, dirty untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf는 모두 `24`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-22` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_baseline.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_trailing_input_included_body_baseline_helpers.rs`를 통해 included-body trailing-input rebuild-from-base fixture, rev2 rewrite, rebuild-from-base/build-meta assert를 공유한다. baseline/reversed leaf는 `23/23`줄만 남았고, host는 `4`줄, helper는 `162`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-22` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_untracked.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_untracked_helpers.rs`를 통해 included-body trailing-input rebuild-from-base sibling-extra-dirty untracked fixture, rev2 rewrite, rebuild-from-base/build-meta assert를 공유한다. follows/precedes leaf는 `30/30`줄만 남았고, host는 `3`줄, helper는 `154`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-22` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_helpers.rs`를 통해 included-body trailing-input rebuild-from-base sibling-extra-dirty unreadable fixture, rev2 rewrite, rebuild-from-base/build-meta assert를 공유한다. interleaved untracked/unreadable, other-interleaved unreadable, plain-dirty-order untracked/unreadable leaf 열 개는 모두 `30`줄만 남았고, root host는 `4`줄, helper는 `170`줄이며 targeted exact lanes `10/10`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_ordering.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_trailing_input_included_body_sibling_ordering_helpers.rs`를 통해 same-page included-body trailing-input sibling-ordering fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/interleaved leaf 네 개는 모두 `25`줄만 남았고, targeted exact lanes `4/4`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_helpers.rs`를 통해 same-page included-body trailing-input sibling-extra-dirty fixture, rev2 rewrite, replay/build-meta assert를 공유한다. untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 열두 개는 모두 `29`줄만 남았고, targeted exact lanes `12/12`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_trailing_input_baseline.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_trailing_input_baseline_helpers.rs`를 통해 same-page trailing-input replay fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/reversed leaf는 `21/21`줄, dirty untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf는 모두 `26`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_helpers.rs`를 통해 same-page trailing-input sibling-extra-dirty fixture, rev2 rewrite, replay/build-meta assert를 공유한다. untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 열두 개는 모두 `27`줄만 남았고, targeted exact lanes `12/12`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_checkpoint_preference_included_body.rs`는 이제 `crates/latexd/tests/support/bibliography_checkpoint_preference_included_body_helpers.rs`를 통해 included-body checkpoint-preference fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/reversed leaf는 `22/22`줄, dirty untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf는 모두 `24`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_replay_checkpoint_preference_included_body_dirty_unreadable_noise.rs`도 기존 `crates/latexd/tests/support/bibliography_multi_replay_checkpoint_preference_helpers.rs` fixture/rewrite/assert path로 접혔다. unreadable-noise included-body `..._follows.rs` / `..._precedes.rs` leaf 두 개는 모두 `38`줄만 남았고, targeted exact lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_replay_checkpoint_preference_toplevel_dirty_unreadable_noise.rs`도 기존 `crates/latexd/tests/support/bibliography_multi_replay_checkpoint_preference_helpers.rs` fixture/rewrite/assert path로 접혔다. unreadable-noise toplevel `..._follows.rs` / `..._precedes.rs` leaf 두 개는 모두 `26`줄만 남았고, targeted exact lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_checkpoint_preference_dirty_only.rs`는 이제 `crates/latexd/tests/support/bibliography_checkpoint_preference_dirty_only_helpers.rs`를 통해 dirty-only checkpoint-preference fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 네 개는 모두 `23`줄만 남았고, targeted exact lanes `4/4`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_semantically_equal_replay_included_body.rs`는 이제 `crates/latexd/tests/support/bibliography_semantically_equal_replay_included_body_helpers.rs`를 통해 semantically-equal included-body replay fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 네 개는 모두 `19`줄만 남았고, targeted exact lanes `4/4`는 green이다
  - `2026-04-18` progress: `smoke/replay_selection_include_boundaries_repeated_include_baseline.rs`는 이제 `crates/latexd/tests/support/replay_selection_include_boundaries_repeated_include_baseline_helpers.rs`를 통해 repeated-include baseline fixture, rev2 rewrite, replay/build-meta assert를 공유한다. same-file/multi-file leaf는 `12/16`줄만 남았고, targeted exact lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/replay_selection_include_boundaries_nested_exit_core.rs`는 이제 `crates/latexd/tests/support/replay_selection_include_boundaries_nested_exit_baseline_helpers.rs`를 통해 nested-exit baseline fixture, rev2 rewrite, replay/build-meta assert를 공유한다. same-file/multi-file baseline leaf는 `11/14`줄만 남았고, targeted exact lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/replay_selection_late_toplevel.rs`는 이제 `crates/latexd/tests/support/replay_selection_late_toplevel_helpers.rs`를 통해 late-toplevel replay fixture, rev2 rewrite, checkpoint-selection/build-meta assert를 공유한다. shipout baseline과 untracked/unreadable noise leaf는 각각 `32/23/19/19/19`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-18` progress: `smoke/external_oracle_custom_compiler_dependency_tracking_fallback_and_depfile.rs`와 `smoke/external_oracle_custom_compiler_dependency_tracking_fls_and_unreadable.rs`도 include-only host가 되었다. dependency-tracking coverage는 fallback/depfile-preference와 fls/unreadable-depfile leaf로 다시 나뉘었고, 새 leaf는 `62/77/72/76`줄이며 targeted exact lanes `4/4`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input.rs`는 이제 `crates/latexd/tests/support/bibliography_semantically_equal_replay_later_trailing_input_helpers.rs`를 통해 later-trailing-input replay fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/reversed와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 여섯 개는 `22/22/24/24/24/24`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_toplevel.rs`는 이제 `crates/latexd/tests/support/bibliography_semantically_equal_replay_later_trailing_input_toplevel_helpers.rs`를 통해 top-level later-trailing-input replay fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/reversed와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 여섯 개는 `24/24/26/26/26/26`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_baseline.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_trailing_noise_baseline_helpers.rs`를 통해 trailing-noise unchanged-tail baseline fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. plain/reversed와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 여덟 개는 모두 `19`줄만 남았고, targeted exact lanes `8/8`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_trailing_noise_baseline_helpers.rs` 공유 경로를 interleaved/other-interleaved 분기에도 노출한다. interleaved/other-interleaved untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 여덟 개는 모두 `19`줄만 남았고, shared fixture, rev2 rewrite, unchanged-tail/build-meta assert 위의 얇은 dirty-order assertion만 유지하며 targeted exact lanes `8/8`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_between_untracked.rs`와 `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_between_unreadable.rs`도 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_trailing_noise_baseline_helpers.rs` 공유 경로로 접혔다. between-them leaf 두 개는 shared fixture, rev2 rewrite, unchanged-tail/build-meta assert 위의 얇은 dirty-order assertion만 남겨 각각 `19`줄이 되었고, targeted exact lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/replay_selection_input_boundaries_same_page_multi_file.rs`도 `crates/latexd/tests/support/replay_selection_input_boundaries_helpers.rs`의 same-page fixture/assert path로 접혔다. leaf는 `154`줄에서 `32`줄로 줄었고, `prepare_same_page_input_boundary_fixture(false)` 위의 얇은 two-input rewrite wrapper만 남았다. targeted exact lane `1/1`은 green이다
  - `2026-04-25` progress: `smoke/replay_selection_input_boundaries_same_page_cp0.rs`도 같은 shared same-page fixture/assert path를 재사용한다. 자체 manifest/fixture/first-build/build-meta assertion을 걷어내고 early toplevel edit + late input rewrite만 남겨 leaf는 `95`줄에서 `35`줄로 줄었으며, targeted CP0 replay-selection exact lane `1/1`은 green이다
  - `2026-04-18` progress: `smoke/replay_selection_input_boundaries_earlier_page_baseline.rs`도 `crates/latexd/tests/support/replay_selection_input_boundaries_helpers.rs`의 earlier-page fixture/assert path로 접혔다. leaf는 `146`줄에서 `31`줄로 줄었고, `prepare_earlier_page_input_boundary_fixture(false)` 위의 얇은 two-input rewrite wrapper만 남았다. targeted exact lane `1/1`은 green이다
  - `2026-04-18` progress: `smoke/reference_variants_appendix_numbering.rs`도 include-only host가 되었다. appendix-numbering coverage는 `..._appendix.rs`, `..._appendices.rs`, `..._chapter.rs`로 나뉘었고, targeted exact lanes `3/3`은 green이다
  - `2026-04-18` progress: `smoke/reference_variants_toc_numbering.rs`도 include-only host가 되었다. toc/label numbering coverage는 `..._subsubsection.rs`, `..._chapter.rs`, `..._optional_title.rs`로 나뉘었고, targeted exact lanes `3/3`은 green이다
  - `2026-04-29` progress: TOC subsubsection, TOC optional-title, appendix-letter reference leaves가 기존 reference-variant internal fixture/compile path를 재사용한다. 세 leaf는 local tempdir/manifest/world/driver setup을 내려놓고 `55/54/53 -> 30/29/28`줄이 되었으며 exact lanes `3/3`은 green이다
  - `2026-04-18` progress: `smoke/reference_variants_theorem_kinds.rs`와 `smoke/internal_baselines_metadata_and_reuse.rs`도 include-only host가 되었다. theorem-kind reference coverage는 `..._claim_example.rs`, `..._axiom_fact_observation.rs`, `..._problem_exercise_question_notation.rs`로, internal-baseline metadata/reuse coverage는 `..._multipage.rs`, `..._preamble.rs`로 나뉘었고, targeted exact lanes `5/5`는 green이다
  - `2026-04-25` progress: `crates/latexd/tests/support/internal_baselines_helpers.rs`가 rev-aware compile path를 제공하면서 internal-baseline metadata/reuse leaf도 같은 fixture setup을 재사용한다. multipage metadata leaf는 `48 -> 27`줄, preamble-checkpoint leaf는 `106 -> 69`줄이 되었고 helper는 `62`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-28` progress: `smoke/internal_baselines_metadata_and_reuse_preamble.rs`는 rev2/rev3 non-semantic build-meta 반복 검증을 test-local macro로 접었다. preamble-checkpoint leaf는 `69 -> 60`줄이 되었고 targeted exact check는 green이다
  - `2026-04-29` progress: `crates/latexd/tests/support/internal_baselines_helpers.rs`는 이제 changed files를 string slice로 받아 support 내부에서 `Utf8PathBuf`로 변환한다. internal-baseline leaf 호출부의 `Utf8PathBuf::from("main.tex")` 반복이 사라졌고 preamble은 `60 -> 58`줄, multipage는 `27 -> 26`줄, bibliography stem-order는 `28`줄이며 affected exact lanes `6/6`는 green이다
  - `2026-04-29` progress: `smoke/internal_baselines_metadata_and_reuse_preamble.rs`는 test-local macro 대신 closure와 build-meta path local을 사용해 nested path construction을 줄였다. preamble-checkpoint leaf는 `58 -> 53`줄이며 exact lane은 green이다
  - `2026-04-18` progress: `smoke/replay_selection_include_boundaries_nested_exit_core_toplevel.rs`와 `smoke/replay_selection_include_boundaries_repeated_include_toplevel.rs`는 이제 각각 `crates/latexd/tests/support/replay_selection_include_boundaries_nested_exit_toplevel_helpers.rs`와 `crates/latexd/tests/support/replay_selection_include_boundaries_repeated_include_toplevel_helpers.rs`를 통해 toplevel include-boundary fixture, rev2 rewrite, replay/build-meta assert를 공유한다. plain/appendix leaf 네 개는 모두 얇은 dirty-file assertion만 남겼고, targeted exact lanes `4/4`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_ordering.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_trailing_input_sibling_ordering_helpers.rs`를 통해 same-page trailing-input sibling-ordering fixture, rev2 rewrite, replay/build-meta assert를 공유한다. baseline/interleaved leaf 네 개는 모두 `23`줄만 남았고, targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_replay_included_body_extra_dirty_unreadable_noise.rs`와 `smoke/bibliography_multi_replay_trailing_input_toplevel_dirty_unreadable_noise.rs`도 include-only host가 되었다. 두 unreadable-noise bibliography replay leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_output_basic_wrappers.rs`도 include-only host가 되었다. bibliography-output wrapper coverage는 `..._href_and_urlprefix.rs`, `..._common_and_newunit.rs`, `..._bibstring_and_acro.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_trailing_input_included_tail_dirty_unreadable_noise.rs`와 `smoke/bibliography_multi_same_page_trailing_input_sibling_ordering_baseline.rs`도 include-only host가 되었다. unreadable tail-dirty replay leaf는 `..._follows.rs`, `..._precedes.rs`로, same-page sibling-ordering baseline family는 `..._plain.rs`, `..._reversed.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_engine_failures_warning_and_spawn.rs`와 `smoke/bibliography_multi_rebuild_late_bibliography_skip_cases.rs`도 include-only host가 되었다. warning/spawn oracle family는 `..._warnings.rs`, `..._spawn_failures.rs`로, late-bibliography skip family는 `..._single_page.rs`, `..._multi_page.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_engine_failures_warning_and_spawn_warnings.rs`도 include-only host가 되었다. warning-diagnostics coverage는 `..._tectonic.rs`, `..._pdflatex.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_baseline_core.rs`와 `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_untracked_other_interleaved_orders.rs`도 include-only host가 되었다. rev5 baseline core family는 `..._shipout_replay.rs`, `..._rebuild_from_base.rs`로, same-page sibling extra-dirty other-interleaved family는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_root_baseline.rs`는 shared rev5 late-change fixture/assert helper를 더 들고 있지 않다. 공용 helper는 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_late_change_helpers.rs`로 옮겼고, smoke leaf는 `298`줄에서 `97`줄로 줄었다. root/included-body late-change exact lanes `10/10`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_included_body_extra_dirty.rs`도 include-only host가 되었다. included-body late-change extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었고, 네 leaf는 shared `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_late_change_helpers.rs` workspace/build/assert path 위의 얇은 dirty-order assertion만 남겨 각각 `19`줄이 되었다. host는 `4`줄이며 targeted exact lanes `4/4`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_noise.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_leading_noise_helpers.rs`를 통해 leading-noise unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 baseline, interleaved, other-interleaved branch 전체에서 공유한다. untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 열여섯 개는 모두 `19`줄만 남았고, targeted exact lanes `16/16`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_trailing_toplevel.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_trailing_toplevel_multi_input_leading_helpers.rs`를 통해 multi-input-leading trailing-toplevel fixture, rev2 rewrite, unchanged-tail/build-meta assert를 dirty-order, interleaved, other-interleaved branch에서 공유한다. 해당 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 열두 개는 `27/29`줄만 남았고, root host는 `13`줄이며 targeted exact lanes `12/12`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_trailing_toplevel.rs`는 이제 같은 `crates/latexd/tests/support/unchanged_tail_noop_trailing_toplevel_multi_input_leading_helpers.rs` helper를 mixed-multi-input trailing-toplevel branch에도 확장해서 쓴다. mixed dirty-order, interleaved, other-interleaved branch의 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 열두 개는 shared fixture/rewrite/build-meta assert 위 thin wrapper만 남겨 모두 `27/29`줄이 되었고, helper는 `193`줄이며 targeted exact lanes `12/12`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_mixed_toplevel_dirty_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_mixed_toplevel_dirty_orders_helpers.rs`를 통해 dirty-order mixed-toplevel unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 baseline/reversed와 untracked/unreadable `..._follows.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 여섯 leaf는 각각 `15/15/17/17/17/17`줄만 남았고, targeted exact lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_primary.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_primary_helpers.rs`를 통해 primary-interleaved leading-toplevel multi-input unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 다섯 leaf는 각각 `21/23/23/23/23`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_other.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_other_helpers.rs`를 통해 other-interleaved leading-toplevel multi-input unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 다섯 leaf는 각각 `24/23/23/23/23`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_helpers.rs`를 통해 mixed-multi-input leading-toplevel interleaved unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 primary와 other-interleaved base 및 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `5`줄, helper는 `169`줄이며 열 개 leaf는 각각 `21/23/23/23/23/21/23/23/23/23`줄만 남았고, targeted exact lanes `10/10`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_helpers.rs`를 통해 mixed-multi-input leading-toplevel dirty-order unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 plain/reversed base 및 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `5`줄, helper는 `169`줄이며 열 개 leaf는 각각 `21/21/23/23/23/23/23/23/23/23`줄만 남았고, targeted exact lanes `10/10`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_interleaved_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_interleaved_orders_helpers.rs`를 통해 leading-toplevel interleaved unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 다섯 leaf는 각각 `16/18/18/18/18`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_other_interleaved_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_other_interleaved_orders_helpers.rs`를 통해 leading-toplevel other-interleaved unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 다섯 leaf는 각각 `22/24/24/24/24`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-22` progress: `smoke/unchanged_tail_noop_leading_toplevel.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_dirty_order_family_helpers.rs`를 통해 leading-toplevel dirty-order unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 baseline plain/reversed와 follows/precedes untracked/unreadable plain/reversed leaf 전체에서 공유한다. root host는 `11`줄, helper는 `169`줄이며 열 개 leaf는 각각 `22/22/24/24/24/24/24/24/24/24`줄만 남았고, targeted exact lanes `10/10`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_helpers.rs`를 통해 dirty-order leading-toplevel multi-input unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 baseline/reversed, noise-follows, precedes plain/reversed leaf 전체에서 공유한다. host는 `5`줄, helper는 `169`줄이며 열 개 leaf는 각각 `17/17/19/19/19/19/19/19/19/19`줄만 남았고, targeted exact lanes `10/10`은 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_mixed_toplevel_interleaved_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_mixed_toplevel_interleaved_orders_helpers.rs`를 통해 interleaved mixed-toplevel unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 다섯 leaf는 각각 `17/19/19/19/19`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-18` progress: `smoke/unchanged_tail_noop_mixed_toplevel_other_interleaved_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_mixed_toplevel_other_interleaved_orders_helpers.rs`를 통해 other-interleaved mixed-toplevel unchanged-tail fixture, rewrite, unchanged-tail/build-meta assert를 base와 untracked/unreadable `..._follows.rs` / `..._precedes.rs` leaf 전체에서 공유한다. host는 `7`줄, helper는 `169`줄이며 다섯 leaf는 각각 `21/23/23/23/23`줄만 남았고, targeted exact lanes `5/5`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_rebuild_late_bibliography_skip_cases.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_late_bibliography_skip_cases_helpers.rs`를 통해 late-bibliography skip-case workspace, rev2 rewrite, reuse/build-meta assert를 공유한다. `..._single_page.rs`와 `..._multi_page.rs` leaf는 fixture-shape assertion만 남겨 각각 `16/46`줄이 되었고, host는 `5`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rewrite_cases.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_late_bibliography_rewrite_cases_helpers.rs`를 통해 late-bibliography rewrite workspace, `refs.bbl` rev2 rewrite, cp0 rebuild build-meta assert를 공유한다. `..._earlier_output.rs`와 `..._late_only_change.rs` leaf는 fixture-specific assertion만 남겨 각각 `73/27`줄이 되었고, host는 `4`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-28` progress: 같은 late-bibliography rewrite helper가 rev1/rev2 `sources.json`도 shared run fixture에 싣는다. `smoke/bibliography_multi_rebuild_late_bibliography_rewrite_cases_earlier_output.rs`는 `73 -> 65`줄, `..._late_only_change.rs`는 `27 -> 24`줄이 되었고 helper는 `102`줄이며 exact rewrite-case checks `2/2`는 green이다
  - `2026-04-29` progress: 같은 rewrite helper는 이제 source payload를 `impl AsRef<str>`로 받아 static bibliography payload에서 `.to_string()` scaffolding을 없앤다. earlier-output leaf는 `65 -> 63`줄, late-only-change leaf는 `24`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rewrite_cases_earlier_output.rs`는 borrowed main path와 cp0/bibliography output start locals를 재사용하고 redundant long assertion message를 걷어냈다. earlier-output leaf는 `63 -> 46`줄이며 exact lane은 green이다
  - `2026-04-18` progress: `smoke/bibliography_features_biblatex_print.rs`는 이제 `crates/latexd/tests/support/bibliography_features_biblatex_print_simple_helpers.rs`를 통해 simple biblatex-print rev1 fixture bootstrap, `output.txt`/`sources.json` load, common rendered-text assertion, aux load를 공유한다. core/smartcite, entries/multicite, heading, `printbibheading` simple leaf 일곱 개는 `24/17/34/24/22/16/16`줄만 남았고, host는 `7`줄, helper는 `91`줄이며 targeted exact lanes `8/8`은 green이다
  - `2026-04-25` progress: `smoke/bibliography_features_biblatex_print_entries_and_multicite.rs`도 include-only host가 되었다. fullcite/bibentry와 multicite coverage는 `..._entries.rs`, `..._multicite.rs`로 나뉘었고 host는 `3`줄, leaf는 `16/16`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_mutations_shifted_and_deleted_deleted.rs`, `smoke/unchanged_tail_mutations_inserted_and_replaced_inserted.rs`, `smoke/unchanged_tail_mutations_inserted_and_replaced_replaced.rs`는 shared unchanged-tail mutation fixture setup과 span-range 계산을 더 들고 있지 않다. 공용 helper는 `crates/latexd/tests/support/unchanged_tail_mutation_helpers.rs`로 옮겼고, 세 smoke leaf는 `206/191/192`줄에서 `111/91/95`줄로 줄었다. targeted exact mutation lanes `4/4`는 green이다
  - `2026-04-17` progress: 네 개의 `smoke/bibliography_multi_replay_checkpoint_preference_*_baseline_{baseline,reversed}.rs` leaf는 shared fixture setup, replay-candidate 선택, 반복 assertion block을 더 들고 있지 않다. 공용 helper는 `crates/latexd/tests/support/bibliography_multi_replay_checkpoint_preference_helpers.rs`로 옮겼고, toplevel leaf는 `216/164`줄에서 `45/41`줄로, included-body leaf는 `178/156`줄에서 `36/36`줄로 줄었다. targeted exact checkpoint-preference baseline lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_checkpoint_preference_page_ordering.rs`와 `smoke/bibliography_multi_same_page_checkpoint_preference_page_ordering.rs`는 shared checkpoint page-ordering fixture/rewrite/build-meta assert를 더 들고 있지 않다. 공용 helper는 `crates/latexd/tests/support/bibliography_multi_checkpoint_preference_page_ordering_helpers.rs`로 옮겼고, 두 smoke leaf는 `164/160`줄에서 `38/57`줄로 줄었다. targeted exact checkpoint page-ordering lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_input_boundaries_earlier_page_toplevel_appendix.rs`, `smoke/replay_selection_input_boundaries_same_page_toplevel_appendix.rs`, `smoke/replay_selection_input_boundaries_earlier_page_toplevel.rs`, `smoke/replay_selection_input_boundaries_same_page_toplevel.rs`는 shared replay-selection fixture setup과 반복 non-aux build-meta assertion을 더 들고 있지 않다. 공용 helper는 `crates/latexd/tests/support/replay_selection_input_boundaries_helpers.rs`로 옮겼고, 네 smoke leaf는 `183/172/164/152`줄에서 `55/56/48/48`줄로 줄었다. targeted exact input-boundary replay-selection lanes `4/4`는 green이다
  - `2026-04-29` progress: input-boundary helper가 rev2 compile/changed-file conversion과 shared edited-suffix generation도 맡는다. same-page toplevel/appendix/multi-file/CP0 leaves는 `48/56/32/35 -> 26/35/19/23`줄, earlier-page toplevel/appendix leaves는 `48/55 -> 26/34`줄이며 affected exact lanes `6/6`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_baseline_core_shipout_replay.rs`와 `smoke/bibliography_multi_rebuild_rev5_baseline_core_rebuild_from_base.rs`는 shared `optioned-bibliography-order-stack` fixture copy와 revision runner를 더 들고 있지 않다. 공용 runner는 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_baseline_core_helpers.rs`로 옮겼고, 두 smoke leaf는 `170/102`줄에서 `96/30`줄로 줄었다. targeted exact rev5 baseline-core lanes `2/2`는 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_rebuild_rev5_baseline.rs`는 이제 smoke tree 안의 shared support helper leaf를 더 들고 있지 않다. 공용 rev1-4 fixture/bootstrap과 rev5 base-rebuild assert helper는 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_baseline_support_helpers.rs`로 옮겼고, host는 `7`줄만 남았으며 기존 `smoke/bibliography_multi_rebuild_rev5_baseline_support.rs` helper leaf는 제거했다. representative targeted exact rev5 baseline/tracked-toplevel lanes `6/6`은 green이다
  - `2026-04-18` progress: `smoke/bibliography_multi_rebuild_rev5_baseline_noise.rs`도 include-only host가 되었다. rev5 baseline-noise coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었고, 네 leaf는 shared `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_baseline_support_helpers.rs` rev1-4 bootstrap과 rev5 base-rebuild assert path 위의 얇은 dirty-order assertion만 남겨 각각 `18`줄이 되었다. host는 `4`줄이며 targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: 네 개의 `smoke/bibliography_multi_replay_checkpoint_preference_*_dirty_untracked_noise_{follows,precedes}.rs` leaf는 shared fixture setup, rewrite, 반복 checkpoint-selection assertion을 더 들고 있지 않다. 기존 `crates/latexd/tests/support/bibliography_multi_replay_checkpoint_preference_helpers.rs`가 untracked-noise variants까지 맡도록 확장했고, toplevel leaf는 `132/165`줄에서 `55/43`줄로, included-body leaf는 `143/157`줄에서 `38/38`줄로 줄었다. targeted exact dirty-untracked checkpoint-preference lanes `4/4`는 green이다
  - `2026-04-29` progress: toplevel dirty-untracked checkpoint-preference follows/precedes leaves가 기존 second-pass compile helper와 shared preamble/replay assertion을 사용한다. toplevel untracked leaves는 `55/43 -> 26/33`줄이며 exact lanes `2/2`는 green이다
  - `2026-04-17` progress: same-page toplevel checkpoint-preference baseline/reversed와 dirty untracked/unreadable leaf 여섯 개는 shared fixture setup, rewrite, 반복 build-meta assertion을 더 들고 있지 않다. 공용 helper는 `crates/latexd/tests/support/bibliography_multi_same_page_checkpoint_preference_toplevel_helpers.rs`로 옮겼고, `smoke/bibliography_multi_same_page_checkpoint_preference_toplevel_baseline.rs`는 include-only host가 되어 `163`줄에서 `3`줄로 줄었고, baseline/reversed leaf는 `35/34`줄, dirty untracked/unreadable leaf는 모두 `37`줄이 되었다. targeted exact same-page toplevel checkpoint-preference lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_ordering_extra_dirty.rs`도 include-only host가 되었다. rev5 input-sibling extra-dirty ordering coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_ordering_baseline.rs`도 include-only host가 되었다. rev5 input-sibling ordering baseline coverage는 `..._baseline.rs`, `..._reversed_dirty_order.rs`, `..._interleaved.rs`, `..._other_interleaved.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_extra_dirty_interleaved.rs`도 include-only host가 되었다. rev5 input-sibling interleaved extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_extra_dirty_other_interleaved.rs`도 include-only host가 되었다. rev5 input-sibling other-interleaved extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_pipeline_success_pdf_latex_tectonic.rs`와 `smoke/bibliography_features_citation_fields_basic_variants.rs`도 include-only host가 되었다. tectonic pdf-latex success family는 `..._support_and_depfile.rs`, `..._retain_toplevel.rs`로, citation-field basic variants는 `..._starred_and_author_year.rs`, `..._capitalized_and_title.rs`, `..._citefield.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_untracked_interleaved_orders.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_untracked_dirty_orders.rs`, `smoke/bibliography_multi_same_page_checkpoint_preference_included_body_dirty_untracked_noise.rs`도 include-only host가 되었다. 세 same-page untracked-noise bibliography replay leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_failures.rs`도 include-only host가 되었다. custom-compiler failure coverage는 `..._exit_details.rs`, `..._artifact_and_spawn.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_failures_exit_details.rs`도 include-only host가 되었다. custom-compiler exit-detail coverage는 `..._stderr.rs`, `..._stdout.rs`, `..._quiet.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_custom_compiler_pipeline_warnings.rs`도 include-only host가 되었다. pipeline-warning coverage는 `..._dvips.rs`, `..._multi_stage.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_ordering_interleaved.rs`, `smoke/bibliography_semantically_equal_replay_included_body_untracked_noise.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_baseline_precedes_and_unreadable_unreadable.rs`도 include-only host가 되었다. interleaved sibling-ordering family는 `..._primary.rs`, `..._other.rs`로, 두 replay-noise leaf는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_reversed.rs`와 `smoke/bibliography_multi_same_page_trailing_input_baseline_untracked.rs`도 include-only host가 되었다. later-trailing replay family는 `..._baseline.rs`, `..._reversed.rs`로, same-page trailing-input untracked family는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_replay_checkpoint_preference_toplevel_dirty_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_reversed_untracked_noise.rs`도 include-only host가 되었다. unreadable-noise checkpoint-preference replay leaf는 `..._follows.rs`, `..._precedes.rs`로, trailing-noise unchanged-tail reversed/untracked leaf는 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_reversed_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_plain_untracked_noise.rs`도 include-only host가 되었다. 두 trailing-noise unchanged-tail leaf는 모두 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_plain_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading_noise_reversed_untracked_noise.rs`도 include-only host가 되었다. 두 unchanged-tail noise leaf는 모두 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_noise_reversed_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading_noise_plain_untracked_noise.rs`도 include-only host가 되었다. 두 leading-noise unchanged-tail leaf는 모두 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_noise_plain_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_precedes_untracked_noise.rs`도 include-only host가 되었다. 두 unchanged-tail leaf는 모두 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_precedes_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_precedes_unreadable_noise.rs`도 include-only host가 되었다. 두 leading-toplevel unchanged-tail leaf는 모두 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_precedes_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_other_interleaved_orders_untracked_noise.rs`도 include-only host가 되었다. leading-toplevel unreadable leaf는 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로, trailing-toplevel other-interleaved untracked leaf는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_other_interleaved_orders_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_interleaved_orders_untracked_noise.rs`도 include-only host가 되었다. 두 trailing-toplevel interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_interleaved_orders_unreadable_noise.rs`와 `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_other_interleaved_orders_untracked_noise.rs`도 include-only host가 되었다. 두 trailing-toplevel interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_interleaved_orders_untracked_noise.rs`와 `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_interleaved_orders_unreadable_noise.rs`도 include-only host가 되었다. 두 trailing-toplevel mixed interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_mixed_toplevel_other_interleaved_orders_untracked_noise.rs`와 `smoke/unchanged_tail_noop_mixed_toplevel_interleaved_orders_untracked_noise.rs`도 include-only host가 되었다. 두 mixed-toplevel interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_primary_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_primary_unreadable_noise.rs`도 include-only host가 되었다. 두 leading-toplevel primary interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_other_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_interleaved_orders_other_unreadable_noise.rs`도 include-only host가 되었다. 두 leading-toplevel other interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_primary_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_primary_unreadable_noise.rs`도 include-only host가 되었다. 두 leading-toplevel mixed primary interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_other_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_interleaved_orders_other_unreadable_noise.rs`도 include-only host가 되었다. 두 leading-toplevel mixed other interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_reversed_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_reversed_unreadable_noise.rs`도 include-only host가 되었다. reversed dirty-order coverage는 `..._untracked_follows.rs`, `..._unreadable_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_plain_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_variants_mixed_multi_input_dirty_orders_plain_unreadable_noise.rs`도 include-only host가 되었다. plain dirty-order coverage는 `..._untracked_follows.rs`, `..._unreadable_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_interleaved_orders_untracked_noise.rs`와 `smoke/unchanged_tail_noop_leading_toplevel_interleaved_orders_unreadable_noise.rs`도 include-only host가 되었다. 두 leading-toplevel interleaved leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_other_interleaved_orders_untracked_noise.rs`, `smoke/unchanged_tail_noop_leading_toplevel_other_interleaved_orders_unreadable_noise.rs`, `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_follows_untracked_noise.rs`, `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_follows_unreadable_noise.rs`도 include-only host가 되었다. other-interleaved pair는 `..._follows.rs`, `..._precedes.rs`로, dirty-order-follows pair는 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_other_interleaved_orders_unreadable_noise.rs`, `smoke/unchanged_tail_noop_mixed_toplevel_other_interleaved_orders_unreadable_noise.rs`, `smoke/unchanged_tail_noop_mixed_toplevel_dirty_orders_reversed_noise_follows.rs`, `smoke/unchanged_tail_noop_mixed_toplevel_dirty_orders_noise_follows.rs`도 include-only host가 되었다. 두 other-interleaved unreadable leaf는 `..._follows.rs`, `..._precedes.rs`로, mixed-toplevel dirty-order noise-follows pair는 `..._untracked_follows.rs`, `..._unreadable_follows.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_mixed_toplevel_interleaved_orders_unreadable_noise.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_reversed_noise_follows.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_noise_follows.rs`, `smoke/bibliography_multi_same_page_checkpoint_preference_toplevel_dirty_untracked_noise.rs`도 include-only host가 되었다. mixed-toplevel interleaved unreadable leaf는 `..._follows.rs`, `..._precedes.rs`로, 두 leading-toplevel dirty-order noise-follows host는 `..._untracked_follows.rs`, `..._unreadable_follows.rs`로, same-page checkpoint-preference toplevel untracked leaf는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_checkpoint_preference_toplevel_untracked.rs`와 `smoke/bibliography_checkpoint_preference_included_body_untracked.rs`도 include-only host가 되었다. 두 checkpoint-preference untracked leaf는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_precedes_other_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_precedes_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_follows_other_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_follows_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_leading_noise_precedes_other_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_leading_noise_precedes_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_leading_noise_follows_other_interleaved.rs`, `smoke/unchanged_tail_noop_with_toplevel_leading_noise_follows_interleaved.rs`도 include-only host가 되었다. with-toplevel interleaved coverage는 `..._untracked_follows.rs`, `..._unreadable_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `16/16`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_interleaved.rs`와 `smoke/unchanged_tail_noop_with_toplevel_trailing_interleaved.rs`도 include-only host가 되었다. 각 host는 `..._interleaved.rs`, `..._other_interleaved.rs` leaf로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-22` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_interleaved.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_leading_interleaved_helpers.rs`를 통해 leading with-toplevel interleaved unchanged-tail fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. interleaved/other-interleaved leaf는 `23/23`줄만 남았고, host는 `3`줄, helper는 `161`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-22` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_interleaved.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_trailing_interleaved_helpers.rs`를 통해 trailing with-toplevel interleaved unchanged-tail fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. interleaved/other-interleaved leaf는 `23/23`줄만 남았고, host는 `3`줄, helper는 `161`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_dirty_order_variants.rs`도 include-only host가 되었다. rev5 sibling dirty-order variants는 `..._baseline.rs`, `..._reversed_dirty_order.rs`, `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_toplevel_reversed.rs`도 include-only host가 되었다. toplevel replay coverage는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_baseline.rs`도 include-only host가 되었다. rev5 baseline coverage는 `..._baseline.rs`, `..._reversed_dirty_order.rs`, `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_untracked.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_baseline.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_plain_dirty_orders_untracked_noise.rs`도 include-only host가 되었다. replay-with-untracked와 included-body-untracked-noise pair는 각각 `..._follows.rs`, `..._precedes.rs`로, included-body baseline pair는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_plain_dirty_orders_unreadable_noise.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_unreadable_dirty_orders.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_unreadable_interleaved_orders.rs`도 include-only host가 되었다. unreadable/dirty-order coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_interleaved_unreadable.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_interleaved_untracked.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_unreadable_other_interleaved_unreadable.rs`도 include-only host가 되었다. included-body interleaved coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_extra_dirty_untracked.rs`, `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_unreadable_other_interleaved_orders.rs`, `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_untracked_dirty_orders.rs`도 include-only host가 되었다. same-page와 included-body other-interleaved/untracked coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_untracked_interleaved_orders.rs`, `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_untracked_other_interleaved_orders.rs`, `smoke/unchanged_tail_noop_with_toplevel_leading_baseline.rs`도 include-only host가 되었다. 두 same-page untracked host는 `..._follows.rs`, `..._precedes.rs`로, leading with-toplevel baseline host는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-22` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_baseline.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_leading_baseline_helpers.rs`를 통해 leading with-toplevel baseline unchanged-tail fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. baseline/reversed leaf는 `19/19`줄만 남았고, host는 `3`줄, helper는 `161`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_baseline.rs`도 include-only host가 되었다. trailing with-toplevel baseline coverage는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-22` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_baseline.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_with_toplevel_trailing_baseline_helpers.rs`를 통해 trailing with-toplevel baseline unchanged-tail fixture, rev2 rewrite, unchanged-tail/build-meta assert를 공유한다. baseline/reversed leaf는 `22/22`줄만 남았고, host는 `4`줄, helper는 `161`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_leading_toplevel_dirty_order_baseline.rs`, `smoke/unchanged_tail_noop_leading_toplevel_variants_multi_input_dirty_orders_baseline_and_reversed.rs`, `smoke/unchanged_tail_noop_mixed_toplevel_dirty_orders_baseline_and_reversed.rs`도 include-only host가 되었다. dirty-order baseline coverage는 모두 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_trailing_toplevel_multi_input_leading_dirty_orders.rs`, `smoke/unchanged_tail_noop_trailing_toplevel_mixed_multi_input_dirty_orders.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_extra_dirty_unreadable.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_extra_dirty_untracked.rs`도 include-only host가 되었다. trailing-toplevel unchanged-tail 두 host는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로, included-body extra-dirty bibliography 두 host는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-22` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_extra_dirty.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_trailing_input_included_body_extra_dirty_helpers.rs`를 통해 included-body trailing-input rebuild-from-base extra-dirty fixture, rev2 rewrite, rebuild-from-base/build-meta assert를 공유한다. untracked/unreadable subhost는 각각 `2`줄, root host는 `3`줄, helper는 `170`줄이며 follows/precedes leaf 네 개는 모두 `29`줄만 남았고, targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_checkpoint_preference_included_body_dirty_unreadable_noise.rs`, `smoke/bibliography_semantically_equal_replay_included_body_unreadable_noise.rs`, `smoke/bibliography_checkpoint_preference_dirty_only_untracked_noise.rs`도 include-only host가 되었다. replay/checkpoint dirty-noise coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering_baseline.rs`, `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering_interleaved.rs`, `smoke/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders_baseline_and_reversed.rs`도 include-only host가 되었다. sibling-ordering과 same-page late-change coverage는 `..._baseline.rs`, `..._reversed_dirty_order.rs`, `..._interleaved.rs`, `..._other_interleaved.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-22` progress: `smoke/bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_trailing_input_included_body_sibling_ordering_helpers.rs`를 통해 included-body trailing-input rebuild-from-base sibling-ordering fixture, rev2 rewrite, rebuild-from-base/build-meta assert를 공유한다. baseline/interleaved subhost는 각각 `2`줄, root host는 `3`줄, helper는 `154`줄이며 baseline/reversed/interleaved/other-interleaved leaf 네 개는 모두 `26`줄만 남았고, targeted exact lanes `4/4`는 green이다
  - `2026-04-22` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders_baseline_and_reversed.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders_baseline_and_reversed_helpers.rs`를 통해 same-page late-change included-body sibling dirty-order fixture, rev2 rewrite, rebuild-from-base/build-meta assert를 공유한다. root host는 `3`줄, helper는 `151`줄이며 baseline/reversed leaf는 `28/28`줄만 남았고, targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_mixed_dirty_orders_helpers.rs`를 통해 mixed multi-input unchanged-tail fixture, rev2 rewrite/noise injection, unchanged-tail/build-meta assert를 공유한다. root host는 `5`줄, helper는 `150`줄이며 baseline/reversed/follows/precedes leaf 여덟 개는 모두 `12-14`줄만 남았고, 전체 family는 `1261 -> 315`줄로 줄었으며 targeted exact lanes `10/10`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_output_textstyle_and_starred.rs`는 이제 `crates/latexd/tests/support/bibliography_output_textstyle_and_starred_helpers.rs`를 통해 bibliography-output wrapper stripping용 공통 manifest/main compile path와 output/sources assert를 공유한다. root host는 `7`줄, helper는 `61`줄이며 `case_and_urlstyle`, `starred_wrappers`, `name_affix` shard는 각각 `66/31/10`줄만 남았고, 전체 family는 `319 -> 175`줄로 줄었으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-24` progress: `smoke/bibliography_output_textstyle_and_starred_case_and_urlstyle.rs`도 include-only host가 되었다. case/textstyle wrapper와 `urlstyle` coverage는 `..._case.rs`, `..._urlstyle.rs`로 나뉘었고 host는 `3`줄, leaf는 `53/10`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-25` progress: `smoke/bibliography_output_textstyle_and_starred_starred_wrappers.rs`도 include-only host가 되었다. starred-case와 starred-formatting coverage는 `..._case.rs`, `..._formatting.rs`로 나뉘었고 host는 `3`줄, leaf는 `10/18`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_engine_failures_xelatex.rs`는 이제 `crates/latexd/tests/support/external_oracle_engine_failures_xelatex_helpers.rs`를 통해 xelatex external-oracle manifest/main/tool-dir/world/driver/compile path를 공유한다. root host는 `5`줄, helper는 `53`줄이며 `smoke/external_oracle_engine_failures_xelatex_spawn_and_warnings.rs`는 `85`줄만 남았고, targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: `smoke/external_oracle_engine_failures.rs`가 xelatex/pdflatex/tectonic stderr/stdout/quiet diagnostics용 shared engine nonzero-exit helper를 포함한다. engine exit-detail leaves 9개는 engine별로 `54/54/53`, `54/54/53`, `55/54/53 -> 14/14/13`줄이 되었고 helper는 `58`줄이며 exact exit-detail lanes `9/9`는 green이다
  - `2026-04-24` progress: `smoke/external_oracle_engine_failures_xelatex_spawn_and_warnings.rs`도 include-only host가 되었다. xelatex spawn-failure/warning coverage는 `..._spawn.rs`, `..._warnings.rs`로 나뉘었고 host는 `3`줄, leaf는 `26/60`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: `smoke/external_oracle_engine_failures_xelatex_spawn_and_warnings_warnings.rs`는 shared external-oracle warning outcome assertion을 재사용한다. xelatex warning leaf는 `53 -> 43`줄이며 exact lane은 green이다
  - `2026-04-24` progress: `smoke/reference_variants_multi_label_cleveref.rs`도 include-only host가 되었다. multi-label cleveref coverage는 `..._subsection_pluralization.rs`, `..._lists.rs`로 나뉘었고 host는 `3`줄, leaf는 `48/49`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_missing_artifacts.rs`는 이제 `crates/latexd/tests/support/external_oracle_missing_artifacts_binary_discovery_helpers.rs`를 통해 binary-discovery용 manifest/main/empty-tools/world/driver/compile path를 공유한다. root host는 `13`줄, helper는 `58`줄이며 `smoke/external_oracle_missing_artifacts_binary_discovery.rs`는 `135 -> 57`줄로 줄었고, family는 `128`줄까지 내려갔으며 targeted exact lanes `3/3`은 green이다
  - `2026-04-25` progress: `smoke/external_oracle_missing_artifacts_binary_discovery.rs`도 include-only host가 되었다. xelatex, pdf-latex fallback, latex-dvips binary-discovery failure coverage는 `..._xelatex.rs`, `..._pdf_latex.rs`, `..._latex.rs`로 나뉘었고 host는 `5`줄, leaf는 `19/19/20`줄이며 targeted exact lanes `3/3`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_semantic_features.rs`는 이제 `crates/latexd/tests/support/bibliography_semantic_features_semantic_index_helpers.rs`를 통해 semantic-index용 공통 manifest/main/extra-files/world/driver/compile path를 공유한다. root host는 `7`줄, helper는 `62`줄이며 `smoke/bibliography_semantic_features_semantic_index.rs`는 `132 -> 83`줄로 줄었고, family는 `152`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/bibliography_semantic_features_semantic_index.rs`도 include-only host가 되었다. semantic-index artifact와 `printbibheading`/`bibintoc` coverage는 `..._artifact.rs`, `..._printbibheading_bibintoc.rs`로 나뉘었고 host는 `3`줄, leaf는 `55/27`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-25` progress: `crates/latexd/tests/support/bibliography_semantic_features_semantic_index_helpers.rs`가 rev-aware compile path를 제공하면서 semantic-index leaf뿐 아니라 backdate/rebuild leaf도 같은 manifest/main/extra-files/world/driver setup을 재사용한다. backdate leaf는 `109 -> 77`줄, rebuild leaf는 `97 -> 67`줄이 되었고 helper는 `70`줄이며 semantic-index `2/2`와 backdate/rebuild `2/2` targeted checks는 green이다
  - `2026-04-28` progress: 같은 semantic-index helper가 rev2 compile과 build-meta load/common assertion도 backdate/rebuild leaf에 제공한다. backdate leaf는 `77 -> 67`줄, rebuild leaf는 `67 -> 54`줄이 되었고 helper는 `87`줄이며 backdate/rebuild exact checks `2/2`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_engine_failures_warning_and_spawn.rs`는 이제 `crates/latexd/tests/support/external_oracle_engine_failures_warning_and_spawn_helpers.rs`를 통해 pdf-latex warning/spawn용 공통 manifest/main/fake-tools/world/driver/compile path를 공유한다. root host는 `5`줄, helper는 `53`줄, warnings host는 `2`줄이며 `smoke/external_oracle_engine_failures_warning_and_spawn_warnings_pdflatex.rs`, `smoke/external_oracle_engine_failures_warning_and_spawn_warnings_tectonic.rs`, `smoke/external_oracle_engine_failures_warning_and_spawn_spawn_failures.rs`는 각각 `58/63/51`줄만 남았고, family는 `232`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-29` progress: pdflatex/tectonic warning-diagnostics fake engine은 multi-line dummy PDF body 대신 shell redirection으로 placeholder PDF를 만든다. warning leaves는 `58/63 -> 53/58`줄이 되었고 exact warning checks `2/2`는 green이다
  - `2026-04-29` progress: pdflatex/tectonic warning leaves는 이제 `external_oracle_engine_failures_warning_and_spawn_helpers.rs`의 shared warning outcome assertion을 재사용한다. leaves는 `53/58 -> 43/48`줄, helper는 `67`줄이며 exact warning checks `2/2`는 green이다
  - `2026-04-29` progress: xelatex warning-diagnostics fake engine도 같은 shell-redirection placeholder PDF path를 사용한다. xelatex warning leaf는 `60 -> 53`줄이 되었고 exact warning check는 green이다
  - `2026-04-24` progress: `smoke/external_oracle_engine_failures_warning_and_spawn_spawn_failures.rs`도 include-only host가 되었다. tectonic/pdflatex fallback spawn-failure coverage는 `..._tectonic.rs`, `..._pdflatex.rs`로 나뉘었고 host는 `3`줄, leaf는 `26/26`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_output_basic_wrappers.rs`는 이제 `crates/latexd/tests/support/bibliography_output_basic_wrappers_helpers.rs`를 통해 bibliography-output basic-wrapper용 공통 manifest/main/refs compile path와 output/sources assert를 공유한다. root host는 `7`줄, helper는 `60`줄이며 `smoke/bibliography_output_basic_wrappers_common_and_newunit.rs`, `smoke/bibliography_output_basic_wrappers_bibstring_and_acro.rs`, `smoke/bibliography_output_basic_wrappers_href_and_urlprefix.rs`는 각각 `41/11/25`줄만 남았고, family는 `144`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-24` progress: `smoke/bibliography_output_basic_wrappers_common_and_newunit.rs`도 include-only host가 되었다. common-wrapper와 newunit/addpunct coverage는 `..._common.rs`, `..._newunit.rs`로 나뉘었고 host는 `3`줄, leaf는 `19/19`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-25` progress: `smoke/bibliography_output_basic_wrappers_href_and_urlprefix.rs`도 include-only host가 되었다. href-target와 urlprefix/bibnamedash coverage는 `..._href.rs`, `..._urlprefix.rs`로 나뉘었고 host는 `3`줄, leaf는 `11/11`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_output_punctuation_and_spacing.rs`는 이제 `crates/latexd/tests/support/bibliography_output_punctuation_and_spacing_helpers.rs`를 통해 bibliography-output punctuation/spacing용 공통 manifest/main/refs compile path와 output/sources assert를 공유한다. root host는 `7`줄, helper는 `63`줄이며 `smoke/bibliography_output_punctuation_and_spacing_parentext_and_dash.rs`, `smoke/bibliography_output_punctuation_and_spacing_low_level_and_super_sub.rs`, `smoke/bibliography_output_punctuation_and_spacing_nolinkurl_path_detokenize.rs`는 각각 `36/34/10`줄만 남았고, family는 `150`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-24` progress: `smoke/bibliography_output_punctuation_and_spacing_parentext_and_dash.rs`도 include-only host가 되었다. parentext/spacing와 dash/slash coverage는 `..._parentext.rs`, `..._dash.rs`로 나뉘었고 host는 `3`줄, leaf는 `17/16`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-25` progress: `smoke/bibliography_output_punctuation_and_spacing_low_level_and_super_sub.rs`도 include-only host가 되었다. low-level punctuation와 super/subscript coverage는 `..._low_level.rs`, `..._super_sub.rs`로 나뉘었고 host는 `3`줄, leaf는 `21/10`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_custom_compiler_failures.rs`는 이제 `crates/latexd/tests/support/external_oracle_custom_compiler_failures_helpers.rs`를 통해 custom-compiler failure용 공통 manifest/main/tool-dir/world/driver/compile path를 공유한다. root host는 `5`줄, helper는 `57`줄이며 `smoke/external_oracle_custom_compiler_failures_artifact_and_spawn.rs`는 `56`줄만 남았고, family는 `118`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: 같은 custom-compiler failure helper가 이제 nonzero exit setup과 stdout/stderr/quiet diagnostic assertion도 맡는다. exit-detail stdout/stderr/quiet leaves는 `55/55/53 -> 14/14/13`줄, helper는 `85`줄이며 exact exit-detail lanes `3/3`은 green이다
  - `2026-04-24` progress: `smoke/external_oracle_custom_compiler_failures_artifact_and_spawn.rs`도 include-only host가 되었다. missing-PDF와 spawn-failure coverage는 `..._missing_pdf.rs`, `..._spawn.rs`로 나뉘었고 host는 `3`줄, leaf는 `35/22`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_root_extra_dirty.rs`는 이제 기존 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_late_change_helpers.rs`를 통해 root extra-dirty quartet용 공통 tempdir/root/driver/build-root prepare와 second-pass compile path를 공유한다. root host는 `5`줄을 유지하고 shared helper는 `289`줄이 되었으며 `smoke/bibliography_multi_rebuild_rev5_late_change_root_extra_dirty.rs`는 `123 -> 79`줄로 줄었고, targeted exact lanes `4/4`는 green이다
  - `2026-04-25` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_root_extra_dirty.rs`도 include-only host가 되었다. untracked/unreadable extra-dirty lanes는 `..._untracked.rs`, `..._unreadable.rs` 아래 follows/precedes leaf로 나뉘었고 root/subhost는 `3/3/3`줄, leaf 네 개는 모두 `19`줄이며 targeted exact lanes `4/4`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_missing_artifacts_output_files.rs`는 이제 `crates/latexd/tests/support/external_oracle_missing_artifacts_output_files_helpers.rs`를 통해 output-file missing-artifact용 공통 manifest/main/tool-dir/world/build-root prepare와 PATH-scoped compile path를 공유한다. root host는 `7`줄, helper는 `57`줄이며 `smoke/external_oracle_missing_artifacts_output_files_dvips_and_dvi.rs`, `smoke/external_oracle_missing_artifacts_output_files_ps2pdf_and_postscript.rs`, `smoke/external_oracle_missing_artifacts_output_files_pdf_outputs.rs`는 각각 `46/47/51`줄만 남았고, family는 `208`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-24` progress: `smoke/external_oracle_missing_artifacts_output_files_dvips_and_dvi.rs`도 include-only host가 되었다. missing-dvips-binary와 missing-DVI coverage는 `..._missing_binary.rs`, `..._missing_dvi.rs`로 나뉘었고 host는 `3`줄, leaf는 `18/27`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/external_oracle_missing_artifacts_output_files_ps2pdf_and_postscript.rs`도 include-only host가 되었다. missing-ps2pdf-binary와 missing-PostScript coverage는 `..._missing_binary.rs`, `..._missing_postscript.rs`로 나뉘었고 host는 `5`줄, leaf는 `19/27`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/external_oracle_missing_artifacts_output_files_pdf_outputs.rs`도 include-only host가 되었다. ps2pdf/xelatex missing-PDF coverage는 `..._ps2pdf.rs`, `..._xelatex.rs`로 나뉘었고 host는 `3`줄, leaf는 `27/24`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/internal_baselines.rs`는 이제 `crates/latexd/tests/support/internal_baselines_helpers.rs`를 통해 internal-baseline용 공통 manifest/main/article.cls/extra-files/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `7`줄, helper는 `54`줄이며 `smoke/internal_baselines_bibliography_and_toc.rs`, `smoke/internal_baselines_source_and_failure.rs`는 각각 `68/38`줄만 남았고, family는 `170`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-24` progress: `smoke/internal_baselines_bibliography_and_toc.rs`도 include-only host가 되었다. bibliography stem-order와 starred-section TOC coverage는 `..._bibliography_stem_order.rs`, `..._starred_section_toc.rs`로 나뉘었고 host는 `3`줄, leaf는 `35/31`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/internal_baselines_source_and_failure.rs`도 include-only host가 되었다. nested-source-span와 VM-diagnostic failure coverage는 `..._source_spans.rs`, `..._vm_diagnostics.rs`로 나뉘었고 host는 `3`줄, leaf는 `26/10`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/reference_variants_autoref_equations_floats.rs`는 이제 `crates/latexd/tests/support/reference_variants_autoref_equations_floats_helpers.rs`를 통해 autoref equation/float/list/caption용 공통 manifest/main/article.cls/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `7`줄, helper는 `49`줄이며 `smoke/reference_variants_autoref_equations_floats_equations.rs`, `smoke/reference_variants_autoref_equations_floats_float_lists.rs`, `smoke/reference_variants_autoref_equations_floats_caption_variants.rs`는 각각 `54/61/66`줄만 남았고, family는 `237`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-24` progress: `smoke/reference_variants_autoref_equations_floats_equations.rs`도 include-only host가 되었다. equation-kind와 align/gather label coverage는 `..._kinds.rs`, `..._align_labels.rs`로 나뉘었고 host는 `3`줄, leaf는 `29/23`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/reference_variants_autoref_equations_floats_float_lists.rs`도 include-only host가 되었다. float-kind와 caption/list coverage는 `..._float_kinds.rs`, `..._captions_and_lists.rs`로 나뉘었고 host는 `3`줄, leaf는 `24/35`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/reference_variants_autoref_equations_floats_caption_variants.rs`도 include-only host가 되었다. `captionof`와 starred-caption coverage는 `..._captionof.rs`, `..._starred.rs`로 나뉘었고 host는 `3`줄, leaf는 `31/34`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_features_citation_fields_basic_variants.rs`는 이제 `crates/latexd/tests/support/bibliography_features_citation_fields_basic_variants_helpers.rs`를 통해 citation-field basic-variant용 공통 manifest/main/article.cls/refs.bbl/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `7`줄, helper는 `51`줄이며 `smoke/bibliography_features_citation_fields_basic_variants_starred_and_author_year.rs`, `smoke/bibliography_features_citation_fields_basic_variants_capitalized_and_title.rs`, `smoke/bibliography_features_citation_fields_basic_variants_citefield.rs`는 각각 `56/49/22`줄만 남았고, family는 `185`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-24` progress: `smoke/bibliography_features_citation_fields_basic_variants_starred_and_author_year.rs`도 include-only host가 되었다. starred-ref/optional-note와 citeauthor/citeyear coverage는 `..._starred_refs.rs`, `..._citeauthor_year.rs`로 나뉘었고 host는 `7`줄, leaf는 `30/23`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/bibliography_features_citation_fields_basic_variants_capitalized_and_title.rs`도 include-only host가 되었다. capitalized-author/year와 cite-title coverage는 `..._capitalized_author_year.rs`, `..._title.rs`로 나뉘었고 host는 `5`줄, leaf는 `23/23`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/reference_variants_basic_refs.rs`는 이제 `crates/latexd/tests/support/reference_variants_basic_refs_helpers.rs`를 통해 basic-reference용 공통 manifest/main/article.cls/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `7`줄, helper는 `47`줄이며 `smoke/reference_variants_basic_refs_eq_and_subref.rs`, `smoke/reference_variants_basic_refs_name_and_title.rs`, `smoke/reference_variants_basic_refs_preferred_titles.rs`는 각각 `46/46/49`줄만 남았고, family는 `195`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-24` progress: `smoke/reference_variants_basic_refs_eq_and_subref.rs`도 include-only host가 되었다. eqref와 subref/subeqref coverage는 `..._eqref.rs`, `..._subref.rs`로 나뉘었고 host는 `3`줄, leaf는 `21/22`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/reference_variants_basic_refs_name_and_title.rs`도 include-only host가 되었다. nameref와 titleref coverage는 `..._nameref.rs`, `..._titleref.rs`로 나뉘었고 host는 `3`줄, leaf는 `21/22`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/reference_variants_theorem_setup.rs`는 이제 `crates/latexd/tests/support/reference_variants_theorem_setup_helpers.rs`를 통해 theorem-setup용 공통 manifest/main/article.cls/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `7`줄, helper는 `49`줄이며 `smoke/reference_variants_theorem_setup_kinds.rs`, `smoke/reference_variants_theorem_setup_newtheorem.rs`, `smoke/reference_variants_theorem_setup_presentation.rs`는 각각 `49/4/52`줄만 남았고, family는 `161`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-24` progress: `smoke/reference_variants_theorem_setup_kinds.rs`도 include-only host가 되었다. algorithm-kind와 theorem-kind coverage는 `..._algorithm.rs`, `..._theorem.rs`로 나뉘었고 host는 `3`줄, leaf는 `23/24`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-24` progress: `smoke/reference_variants_theorem_setup_presentation.rs`도 include-only host가 되었다. theoremstyle-declaration와 theorem/proof-header coverage는 `..._theoremstyle.rs`, `..._headers.rs`로 나뉘었고 host는 `3`줄, leaf는 `25/25`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: 네 `smoke/reference_variants_newtheorem_*` leaf도 shared theorem-setup fixture/compile helper를 재사용한다. defined/shared/section-scoped/builtin-override leaves는 `60/53/51/54 -> 35/28/26/29`줄이 되었고 targeted exact lanes `4/4`는 green이다
  - `2026-04-24` progress: `smoke/reference_variants_basic_refs_preferred_titles.rs`도 include-only host가 되었다. section-title와 float-caption preferred-title coverage는 `..._section.rs`, `..._float.rs`로 나뉘었고 host는 `3`줄, leaf는 `23/23`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_features_citation_fields_identifiers.rs`는 이제 `crates/latexd/tests/support/bibliography_features_citation_fields_identifiers_helpers.rs`를 통해 citation-field identifier용 공통 manifest/main/article.cls/refs.bbl/world/build-root prepare와 rev1 internal compile path를 공유한다. top host와 두 subhost는 각각 `5/3/3`줄이 되었고 helper는 `51`줄이며 `smoke/bibliography_features_citation_fields_identifiers_url_and_number_citeurl.rs`, `smoke/bibliography_features_citation_fields_identifiers_url_and_number_bibfield_url.rs`, `smoke/bibliography_features_citation_fields_identifiers_url_and_number_citenum.rs`, `smoke/bibliography_features_citation_fields_identifiers_identifier_and_date_citefield.rs`, `smoke/bibliography_features_citation_fields_identifiers_identifier_and_date_identifiers.rs`, `smoke/bibliography_features_citation_fields_identifiers_identifier_and_date_date.rs`는 각각 `32/33/22/30/44/41`줄만 남았고, family는 `264`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-23` progress: `smoke/reference_variants_theorem_kinds.rs`는 이제 `crates/latexd/tests/support/reference_variants_theorem_kinds_helpers.rs`를 통해 theorem-kind용 공통 manifest/main/article.cls/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `7`줄, helper는 `49`줄이며 `smoke/reference_variants_theorem_kinds_claim_example.rs`, `smoke/reference_variants_theorem_kinds_axiom_fact_observation.rs`, `smoke/reference_variants_theorem_kinds_problem_exercise_question_notation.rs`는 각각 `24/24/26`줄만 남았고, family는 `130`줄까지 내려갔으며 targeted exact lanes `3/3`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_features_citation_fields_metadata.rs`는 이제 `crates/latexd/tests/support/bibliography_features_citation_fields_metadata_helpers.rs`를 통해 citation-field metadata용 공통 manifest/main/article.cls/refs.bbl/world/build-root prepare와 rev1 internal compile path를 공유한다. root host는 `5`줄, helper는 `51`줄이며 `smoke/bibliography_features_citation_fields_metadata_bibinfo.rs`, `smoke/bibliography_features_citation_fields_metadata_generic.rs`, `smoke/bibliography_features_citation_fields_metadata_bibfield.rs`는 각각 `43/30/40`줄만 남았고, family는 `169`줄까지 내려갔으며 targeted exact lanes `3/3`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_features_natbib_variants.rs`는 이제 `crates/latexd/tests/support/bibliography_features_natbib_variants_helpers.rs`를 통해 natbib용 공통 manifest/main/article.cls/refs.bbl/world/build-root prepare, rev1 internal compile path, output/source assert를 공유한다. top host와 두 subhost는 각각 `5/3/3`줄이 되었고 helper는 `63`줄이며 `smoke/bibliography_features_natbib_variants_textual_basic.rs`, `smoke/bibliography_features_natbib_variants_textual_capitalized.rs`, `smoke/bibliography_features_natbib_variants_textual_notes.rs`, `smoke/bibliography_features_natbib_variants_other_variants_citetext.rs`, `smoke/bibliography_features_natbib_variants_other_variants_starred.rs`, `smoke/bibliography_features_natbib_variants_other_variants_parenthetical.rs`는 각각 `14/14/14/14/14/14`줄만 남았고, family는 `152`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_features_year_aliases.rs`는 이제 `crates/latexd/tests/support/bibliography_features_year_aliases_helpers.rs`를 통해 year/alias용 공통 manifest/main/article.cls/refs.bbl/world/build-root prepare, rev1 internal compile path, output/source assert를 공유한다. root host는 `6`줄, helper는 `67`줄이며 `smoke/bibliography_features_year_aliases_citeyearpar.rs`, `smoke/bibliography_features_year_aliases_natexlab.rs`, `smoke/bibliography_features_year_aliases_citefullauthor.rs`, `smoke/bibliography_features_year_aliases_alias_variants.rs`는 각각 `14/12/14/17`줄만 남았고, family는 `130`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_features_biblatex_print_preamble_workflow.rs`도 small include host가 되었다. split-preamble workflow coverage는 이제 `crates/latexd/tests/support/bibliography_features_biblatex_print_preamble_workflow_helpers.rs`를 통해 fixture copy, overlay/delete replay, rev8 `output.txt`/`sources.json`/`build-meta.json` load를 공유하고, `smoke/bibliography_features_biblatex_print_preamble_workflow_render_output.rs`, `smoke/bibliography_features_biblatex_print_preamble_workflow_build_meta.rs`는 각각 `56/16`줄만 남았다. host는 `4`줄, helper는 `139`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: `smoke/bibliography_features_biblatex_print_preamble_workflow_render_output.rs`는 expected/stale render strings와 반복 debug context를 locals로 모아 rev8 output/source-span context를 assertion마다 다시 쓰지 않는다. render-output leaf는 `56 -> 31`줄이며 exact workflow lane은 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_mutations_shifted_and_deleted_shifted.rs`도 small include host가 되었다. shifted-tail coverage는 이제 `crates/latexd/tests/support/unchanged_tail_mutations_shifted_helpers.rs`를 통해 rev1/rev2 shifted mutation setup과 `build-meta.json` load를 공유하고, `smoke/unchanged_tail_mutations_shifted_and_deleted_shifted_tail_and_patches.rs`, `smoke/unchanged_tail_mutations_shifted_and_deleted_shifted_build_meta_and_checkpoints.rs`는 각각 `43/75`줄만 남았다. host는 `4`줄, helper는 `97`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-28` progress: shifted unchanged-tail build-meta assertion도 같은 shifted helper로 올려 두 shifted-tail leaf가 함께 검증한다. `smoke/unchanged_tail_mutations_shifted_and_deleted_shifted_tail_and_patches.rs`는 `46`줄, `smoke/unchanged_tail_mutations_shifted_and_deleted_shifted_build_meta_and_checkpoints.rs`는 `75 -> 56`줄이 되었고 helper는 `124`줄이며 `shifted_unchanged_tail` filter `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_rev5_baseline_core_shipout_replay.rs`도 small include host가 되었다. semantically-equal shipout-replay coverage는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_baseline_core_shipout_replay_helpers.rs`를 통해 rev3/rev4 `output.txt`/`build-meta.json`/checkpoint bootstrap을 공유하고, `smoke/bibliography_multi_rebuild_rev5_baseline_core_shipout_replay_output.rs`, `smoke/bibliography_multi_rebuild_rev5_baseline_core_shipout_replay_metadata.rs`는 각각 `20/63`줄만 남았다. host는 `4`줄, helper는 `43`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-29` progress: 같은 shipout-replay helper가 이제 rev3/rev4 build-meta, reused checkpoint, unchanged-tail, page-id, reused-artifact invariants까지 검증한 뒤 output strings만 반환한다. metadata leaf는 `63 -> 4`줄, output leaf는 `20`줄, helper는 `95`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_mutations_shifted_and_deleted_deleted.rs`도 small include host가 되었다. deleted-page unchanged-tail coverage는 이제 `crates/latexd/tests/support/unchanged_tail_mutations_deleted_helpers.rs`를 통해 rev2 delete mutation setup과 `build-meta.json` load를 공유하고, `smoke/unchanged_tail_mutations_shifted_and_deleted_deleted_tail_and_patches.rs`, `smoke/unchanged_tail_mutations_shifted_and_deleted_deleted_build_meta.rs`는 각각 `50/23`줄만 남았다. host는 `4`줄, helper는 `66`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_rev5_baseline_sibling_dirty.rs`도 small include host가 되었다. sibling-dirty rev5 rebuild coverage는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_baseline_sibling_dirty_helpers.rs`를 통해 rev5 setup/compile bootstrap을 공유하고, `smoke/bibliography_multi_rebuild_rev5_baseline_sibling_dirty_plain_dirty_order.rs`, `smoke/bibliography_multi_rebuild_rev5_baseline_sibling_dirty_reversed_dirty_order.rs`는 각각 `18/18`줄만 남았다. host는 `4`줄, helper는 `38`줄이며 shard 전체는 `78`줄까지 내려갔고 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_precedes.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_dirty_orders_precedes_helpers.rs`를 통해 multi-input precedes용 공통 fixture, nonrendering rewrite, untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. host는 `4`줄, helper는 `164`줄이며 `smoke/unchanged_tail_noop_multi_input_dirty_orders_precedes_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_dirty_orders_precedes_unreadable_noise.rs`는 각각 `18/18`줄만 남았고, small family는 `204`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_between_noise_untracked_noise.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_between_noise_untracked_noise_helpers.rs`를 통해 multi-input between-noise용 공통 fixture, nonrendering rewrite, untracked dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. host는 `4`줄, helper는 `157`줄이며 `smoke/unchanged_tail_noop_multi_input_between_noise_untracked_noise_interleaved.rs`, `smoke/unchanged_tail_noop_multi_input_between_noise_untracked_noise_other_interleaved.rs`는 각각 `21/21`줄만 남았고, small family는 `203`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_between_noise_unreadable_noise.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_between_noise_unreadable_noise_helpers.rs`를 통해 multi-input between-noise용 공통 fixture, nonrendering rewrite, unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. host는 `4`줄, helper는 `157`줄이며 `smoke/unchanged_tail_noop_multi_input_between_noise_unreadable_noise_interleaved.rs`, `smoke/unchanged_tail_noop_multi_input_between_noise_unreadable_noise_other_interleaved.rs`는 각각 `21/21`줄만 남았고, small family는 `203`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_mixed_between_noise.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_mixed_between_noise_helpers.rs`를 통해 mixed-between-noise용 공통 fixture, mixed nonrendering rewrite, untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `5`줄이고 `smoke/unchanged_tail_noop_multi_input_mixed_between_noise_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_between_noise_unreadable_noise.rs` subhost는 각각 `2/2`줄만 남았으며, 네 개 `..._interleaved.rs` / `..._other_interleaved.rs` leaf는 모두 `21`줄이 되었다. helper는 `171`줄이고 small family는 `264`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_leading_between_noise.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_leading_between_noise_helpers.rs`를 통해 leading-between-noise용 공통 fixture, leading-only nonrendering rewrite, untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `5`줄이고 `smoke/unchanged_tail_noop_multi_input_leading_between_noise_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_leading_between_noise_unreadable_noise.rs` subhost는 각각 `2/2`줄만 남았으며, 네 개 `..._interleaved.rs` / `..._other_interleaved.rs` leaf는 모두 `24`줄이 되었다. helper는 `171`줄이고 small family는 `276`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes_helpers.rs`를 통해 leading-precedes용 공통 fixture, leading-only nonrendering rewrite, untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `5`줄이고 `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes_unreadable_noise.rs` subhost는 각각 `2/2`줄만 남았으며, 네 개 `..._plain_dirty_order.rs` / `..._reversed_dirty_order.rs` leaf는 모두 `24`줄이 되었다. helper는 `171`줄이고 small family는 `276`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_helpers.rs`를 통해 baseline-follows용 공통 fixture, trailing-comment nonrendering rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_baseline_and_reversed.rs`, `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_reversed_noise_follows.rs`, `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_noise_follows.rs` subhost는 각각 `5/2/2`줄만 남았으며, 여섯 leaf는 `22/23/24/24/24/24`줄이 되었다. helper는 `173`줄이고 small family는 `330`줄까지 내려갔으며 targeted exact lanes `6/6`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_helpers.rs`를 통해 leading-baseline-follows용 공통 fixture, trailing-vs-leading rewrite mode 분기, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_baseline_and_reversed.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_reversed_noise_follows.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_noise_follows.rs` subhost는 각각 `7/2/2`줄만 남았으며, 여섯 leaf는 `25/26/27/27/27/27`줄이 되었다. helper는 `195`줄이고 small family는 `372`줄까지 내려갔으며 targeted exact lanes `6/6`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_ordering_multi_file_dirty_order_helpers.rs`를 통해 multi-file dirty-order용 공통 fixture, main/body trailing-comment rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order_untracked_noise.rs`, `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order_unreadable_noise.rs` subhost는 각각 `3/3`줄만 남았으며, `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order_base.rs`와 네 개 `..._follows.rs` / `..._precedes.rs` leaf는 `23/24/24/24/24`줄이 되었다. helper는 `165`줄이고 small family는 `297`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_ordering_double_leading_dirty_order_helpers.rs`를 통해 double-leading dirty-order용 공통 fixture, main/body leading-comment rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order_untracked_noise.rs`, `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order_unreadable_noise.rs` subhost는 각각 `3/3`줄만 남았으며, `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order_base.rs`와 네 개 `..._follows.rs` / `..._precedes.rs` leaf는 `23/24/24/24/24`줄이 되었다. helper는 `165`줄이고 small family는 `297`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이고 `reused_checkpoint_id` / `start_checkpoint_id`가 `None`이어야 하는 stricter contract도 그대로 유지된다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_tail_cleanup_multi_file.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_tail_cleanup_multi_file_helpers.rs`를 통해 tail-cleanup multi-file용 공통 fixture, main/body trailing-comment rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_tail_cleanup_multi_file_base_and_follows.rs`, `smoke/unchanged_tail_noop_tail_cleanup_multi_file_precedes.rs`, `smoke/unchanged_tail_noop_tail_cleanup_multi_file_interleaved_between.rs`, `smoke/unchanged_tail_noop_tail_cleanup_multi_file_other_interleaved.rs` subhost는 각각 `3/3/3/3`줄만 남았으며 `smoke/unchanged_tail_noop_tail_cleanup_multi_file_base_and_follows_baseline_and_untracked.rs` subhost는 `2`줄만 남았다. 아홉 leaf는 `12/17/17/17/17/17/17/17/17`줄이 되었고 helper는 `165`줄이며 small family는 `334`줄까지 내려갔고 targeted exact lanes `9/9`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_tail_cleanup_double_leading.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_tail_cleanup_double_leading_helpers.rs`를 통해 tail-cleanup double-leading용 공통 fixture, main/body leading-comment rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_tail_cleanup_double_leading_base_and_follows.rs` subhost는 `3`줄, `smoke/unchanged_tail_noop_tail_cleanup_double_leading_base_and_follows_baseline_and_untracked.rs` subhost는 `2`줄, `smoke/unchanged_tail_noop_tail_cleanup_double_leading_precedes.rs`와 `smoke/unchanged_tail_noop_tail_cleanup_double_leading_interleaved_between.rs` subhost는 각각 `2/2`줄만 남았다. 일곱 leaf는 `18/20/20/20/20/20/20`줄이 되었고 helper는 `165`줄이며 small family는 `319`줄까지 내려갔고 targeted exact lanes `7/7`은 green이며 stricter `reused_checkpoint_id == None` / `start_checkpoint_id == None` / `start_page_index == 0` contract도 그대로 유지된다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_tail_cleanup_only_dirty.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_tail_cleanup_only_dirty_helpers.rs`를 통해 only-dirty용 공통 fixture, only-dirty marker 생성, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `4`줄만 남았고 `smoke/unchanged_tail_noop_tail_cleanup_only_dirty_untracked.rs`, `smoke/unchanged_tail_noop_tail_cleanup_only_dirty_unreadable.rs` leaf는 각각 `12/12`줄이 되었다. helper는 `162`줄이고 small family는 `190`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이고 preamble checkpoint reuse contract도 그대로 유지된다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_ordering_double_leading_other_interleaved.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_ordering_double_leading_other_interleaved_helpers.rs`를 통해 double-leading other-interleaved용 공통 fixture, main/body leading-comment rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `5`줄만 남았고 `smoke/unchanged_tail_noop_ordering_double_leading_other_interleaved_untracked_between.rs`, `smoke/unchanged_tail_noop_ordering_double_leading_other_interleaved_unreadable_between.rs` leaf는 각각 `25/25`줄이 되었다. helper는 `163`줄이고 small family는 `218`줄까지 내려갔으며 targeted exact lanes `2/2`는 green이고 stricter `reused_checkpoint_id == None` / `start_checkpoint_id == None` / `start_page_index == 0` contract도 그대로 유지된다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_ordering_mixed_leading.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_ordering_mixed_leading_helpers.rs`를 통해 mixed-leading용 공통 fixture, input-leading vs toplevel-leading rewrite mode, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `5`줄이고 `smoke/unchanged_tail_noop_ordering_mixed_leading_baseline.rs`, `smoke/unchanged_tail_noop_ordering_mixed_leading_reversed.rs` subhost는 각각 `2/2`줄만 남았으며, 네 개 `smoke/unchanged_tail_noop_ordering_mixed_leading_*_{input,toplevel}.rs` leaf는 `18/19/19/19`줄이 되었다. helper는 `180`줄이고 small family는 `264`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이고 input-leading은 checkpoint reuse를 유지하지만 toplevel-leading은 `start_checkpoint_id == None`을 유지하는 계약도 그대로다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_single_source_trailing_input.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_single_source_trailing_input_helpers.rs`를 통해 single-source trailing-input용 공통 fixture, trailing-input rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_single_source_trailing_input_untracked_noise.rs`, `smoke/unchanged_tail_noop_single_source_trailing_input_unreadable_noise.rs` subhost는 각각 `2/2`줄만 남았으며, base와 네 개 `..._follows.rs` / `..._precedes.rs` leaf는 `13/19/19/19/19`줄이 되었다. helper는 `163`줄이고 small family는 `263`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_toplevel.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_toplevel_helpers.rs`를 통해 top-level no-op용 공통 fixture, toplevel rewrite, optional untracked/unreadable dirty marker, second-pass compile, unchanged-tail reuse assert를 공유한다. top host는 `7`줄이고 `smoke/unchanged_tail_noop_toplevel_untracked_noise.rs`, `smoke/unchanged_tail_noop_toplevel_unreadable_noise.rs` subhost는 각각 `2/2`줄만 남았으며, base와 네 개 `..._follows.rs` / `..._precedes.rs` leaf는 `8/12/12/12/12`줄이 되었다. helper는 `162`줄이고 small family는 `229`줄까지 내려갔으며 targeted exact lanes `5/5`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_noop_single_source.rs`는 이제 `crates/latexd/tests/support/unchanged_tail_noop_single_source_leading_helpers.rs`를 통해 single-source leading/toplevel용 공통 fixture, input-vs-toplevel rewrite target, leading-vs-mixed shape, second-pass compile, unchanged-tail reuse assert를 공유한다. root host는 `7`줄이고 `smoke/unchanged_tail_noop_single_source_leading_input.rs`, `smoke/unchanged_tail_noop_single_source_toplevel.rs` subhost는 각각 `2/2`줄만 남았으며, leading/mixed input과 toplevel leaf는 `8/12/9/13`줄이 되었다. helper는 `222`줄이고 small family는 `275`줄까지 내려갔으며 targeted exact lanes `4/4`는 green이고 input target은 checkpoint reuse를 유지하지만 toplevel target은 `start_checkpoint_id == None` contract도 그대로 유지한다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_trailing_input_root.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_trailing_input_root_helpers.rs`를 통해 root trailing-input rebuild-from-base용 공통 fixture, semantic `.bbl`/tail rewrite, optional untracked/unreadable dirty marker, second-pass compile, executed-source/build-meta assert를 공유한다. root host는 `4`줄이고 baseline/extra-dirty host는 `3/2`줄, untracked/unreadable subhost는 각각 `3/3`줄만 남았으며, 여섯 baseline/reversed/follows/precedes leaf는 모두 `8`줄이 되었다. helper는 `219`줄이고 small family는 `282`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_same_page_late_change_included_body.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_late_change_included_body_baseline_helpers.rs`를 통해 included-body same-page late-change baseline/tail-noise rebuild-from-base용 공통 fixture, semantic `.bbl`/tail rewrite, optional untracked/unreadable dirty marker, second-pass compile, executed-source/build-meta assert를 공유한다. included-body host는 `9`줄이고 baseline/tail-noise host는 `5/3`줄, baseline-and-reversed/unreadable subhost는 `6/5`줄만 남았으며, 여섯 baseline/reversed/follows/precedes leaf는 모두 `8`줄이 되었다. helper는 `224`줄이고 small family는 `300`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_same_page_late_change_root.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_same_page_late_change_root_baseline_helpers.rs`를 통해 root same-page late-change baseline/tail-noise rebuild-from-base용 공통 fixture, semantic `.bbl`/tail rewrite, optional untracked/unreadable dirty marker, second-pass compile, executed-source/build-meta assert를 공유한다. root host는 `9`줄이고 baseline/tail-noise host는 `3/3`줄, baseline-and-reversed/unreadable subhost는 `3/2`줄만 남았으며, 여섯 baseline/reversed/follows/precedes leaf는 모두 `8`줄이 되었다. helper는 `215`줄이고 small family는 `283`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders.rs`는 이제 기존 `crates/latexd/tests/support/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders_baseline_and_reversed_helpers.rs` 경로를 untracked/unreadable sibling dirty-order noise 변형에도 재사용한다. 네 noise leaf는 각각 `122`줄에서 `8`줄로 줄었고 기존 baseline/reversed leaf는 `28/28`줄을 유지한다. helper는 `222`줄이고 small family는 `717`줄에서 `332`줄까지 내려갔으며 targeted exact lanes `6/6`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved.rs`도 같은 included-body sibling helper를 interleaved/other-interleaved dirty-order 변형에 재사용한다. 열 개 interleaved leaf는 `120/120/122x8`줄에서 모두 `8`줄로 줄었고, interleaved file family는 `1258`줄에서 `122`줄까지 내려갔다. 공유 helper는 `366`줄이 되었으며 targeted exact lanes `10/10`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_dirty_orders.rs`와 `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved.rs`는 이제 기존 `crates/latexd/tests/support/bibliography_multi_same_page_late_change_root_baseline_helpers.rs` 경로를 sibling `.bbl` dirty-order와 interleaved dirty-order 변형에도 재사용한다. 여섯 dirty-order leaf는 `120/120/117x4`줄에서 모두 `8`줄로 줄었고, 열 개 interleaved leaf는 `115/115/117x8`줄에서 모두 `8`줄로 줄었다. root sibling smoke 파일들은 `1907`줄에서 `161`줄까지 내려갔고 shared root helper는 `417`줄이 되었으며 targeted exact lanes `16/16`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_helpers.rs`를 통해 included-file late-bibliography rebuild-from-base fixture, body/appendix rewrite, optional untracked/unreadable dirty marker, executed-source/build-meta assert를 공유한다. 열 개 baseline/tracked/noise leaf는 `114/111/111/106/106/120/121x4`줄에서 모두 `8`줄로 줄었고 smoke file family는 `1171`줄에서 `103`줄까지 내려갔다. helper는 `254`줄이며 targeted exact lanes `10/10`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_toplevel.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_late_bibliography_rebuild_toplevel_helpers.rs`를 통해 toplevel late-bibliography rebuild-from-base fixture, `.bbl` rewrite, optional untracked/unreadable dirty marker, executed-source/build-meta assert를 공유한다. 네 untracked/unreadable follows/precedes leaf는 `112/109/109/109`줄에서 모두 `8`줄로 줄었고 smoke file family는 `445`줄에서 `40`줄까지 내려갔다. helper는 `159`줄이며 targeted exact lanes `4/4`는 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel.rs`는 이제 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_tracked_toplevel_helpers.rs`를 통해 rev5 tracked-toplevel rebuild-from-base용 rev4 fixture bootstrap, rev5 overlay, tracked `main.tex` rewrite, dirty marker 생성, output/source/build-meta assert를 공유한다. 22개 baseline/noise/sibling-ordering/interleaved leaf는 기존 dirty-file 순서를 유지하면서 얇은 호출부만 남겼고 smoke file family는 `1267`줄에서 `291`줄까지 내려갔다. helper는 `66`줄이며 targeted exact lanes `22/22`는 green이다
  - `2026-04-25` progress: 남아 있던 rev5 tracked-toplevel multi-test subhost도 include-only가 되었다. `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_baseline_baseline.rs`, `..._baseline_untracked.rs`, `..._baseline_unreadable.rs`, `..._sibling_ordering_baseline.rs`, `..._sibling_ordering_interleaved.rs`는 각각 base/reversed, follows/precedes, primary/other leaf로 나뉘었고 host는 `3/3/3/3/5`줄, leaf는 `5/5/9x8`줄만 남았다. broader `later_tracked_toplevel_change` smoke filter `22/22`는 green이다
  - `2026-04-23` progress: `smoke/replay_selection_late_input_removal.rs`는 이제 `crates/latexd/tests/support/replay_selection_late_input_removal_helpers.rs`를 통해 late `\input` fixture setup, input removal mutation, retained/deleted tail handling, optional untracked/unreadable dirty marker, page/build-meta assert를 공유한다. 15개 deleted/file-remains/retained-dirty baseline/noise leaf는 기존 dirty-file 순서를 유지하면서 얇은 호출부만 남겼고 smoke file family는 `1667`줄에서 `174`줄까지 내려갔다. helper는 `141`줄이며 targeted exact lanes `15/15`는 green이다
  - `2026-04-25` progress: 같은 late-input helper가 이제 `smoke/replay_selection_late_input_page_replay.rs`도 담당한다. page-replay leaf와 removal runner가 shared late `\input` fixture와 non-aux build-meta assertion을 함께 쓰면서 page-replay leaf는 `107`줄에서 `39`줄로 줄었고 helper는 `183`줄이며 `late_input` filter `21/21`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_fixpoint_reuse.rs`는 이제 `crates/latexd/tests/support/bibliography_fixpoint_reuse_helpers.rs`를 통해 semantic-bibliography fixpoint reuse용 fixture setup, rev1 checkpoint load, optional `refs.bbl` rewrite 또는 `notes.txt` dirty marker, unchanged-tail assert, build-meta assert를 공유한다. 여섯 toplevel/included-body replay-checkpoint와 tail-noise leaf는 얇은 호출부만 남겼고 smoke file family는 `681`줄에서 `68`줄까지 내려갔다. helper는 `221`줄이며 broader `bibliography` smoke filter는 `287/287` green이다
  - `2026-04-23` progress: `smoke/replay_selection_input_boundaries_late_input_noise.rs`는 이제 기존 `crates/latexd/tests/support/replay_selection_input_boundaries_helpers.rs`를 late-input dirty-noise ordering coverage에도 재사용한다. 네 untracked/unreadable follows/precedes leaf는 late `\input` fixture setup, rev1 checkpoint 선택, optional untracked/unreadable dirty marker, dirty-file ordering, build-meta assert를 공유하면서 `90/90/90/110`줄에서 `9/9/9/8`줄로 줄었고, late-input-noise smoke files는 `386`줄에서 `41`줄까지 내려갔다. shared input-boundaries helper는 `481`줄이며 targeted late-input edit checks `4/4`는 green이다
  - `2026-04-23` progress: `smoke/replay_selection_input_boundaries_toplevel_exit.rs`는 이제 `crates/latexd/tests/support/replay_selection_input_boundaries_toplevel_exit_helpers.rs`를 통해 baseline/clamp top-level input-exit replay fixture, rev1 exit-boundary checkpoint 선택, main-file rewrite, build-meta assert를 공유한다. baseline과 last-page clamp leaf는 `95/109`줄에서 `5/7`줄로 줄었고, toplevel-exit smoke files는 `250`줄에서 `60`줄까지 내려갔다. helper는 `131`줄이며 full `toplevel_input_exit` smoke filter `4/4`는 green이다
  - `2026-04-23` progress: `smoke/unchanged_tail_mutations_inserted_and_replaced.rs`는 이제 기존 `crates/latexd/tests/support/unchanged_tail_mutation_helpers.rs`를 inserted/replaced stable-tail mutation coverage에도 재사용한다. 두 leaf는 shrink-input mutation span 선택, rev2 compile, unchanged-tail assert, page-patch artifact check, build-meta assert를 공유하면서 `91/95`줄에서 `7/7`줄로 줄었고, inserted/replaced smoke files는 `189`줄에서 `17`줄까지 내려갔다. shared mutation helper는 `329`줄이며 neighboring stable-tail mutation filter `3/3`은 green이다
  - `2026-04-23` progress: `smoke/bibliography_multi_rebuild_rev5_late_change_root_baseline.rs`는 이제 기존 `crates/latexd/tests/support/bibliography_multi_rebuild_rev5_late_change_helpers.rs`의 rev5 late-change workspace와 rebuild assertion helper를 재사용한다. root semantic-change fixture inline duplication을 제거해 baseline leaf는 helper 증가 없이 `97`줄에서 `28`줄로 줄었고, broader `late_multi_bibliography_semantic_change` smoke filter `10/10`은 green이다
  - `2026-04-23` progress: `smoke/external_oracle_custom_compiler_pipeline_warnings.rs`는 이제 `crates/latexd/tests/support/external_oracle_custom_compiler_pipeline_warnings_helpers.rs`를 통해 fake `latex -> dvips -> ps2pdf` warning-pipeline setup, PATH isolation, compile execution, diagnostic-message assert를 공유한다. dvips-only와 multi-stage warning leaf는 `73/107`줄에서 `5/7`줄로 줄었고, smoke files는 `182`줄에서 `16`줄까지 내려갔다. helper는 `152`줄이며 targeted warning checks `2/2`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_custom_compiler_preview_and_warnings.rs`는 이제 `crates/latexd/tests/support/external_oracle_custom_compiler_warning_stream_helpers.rs`를 통해 custom-compiler stdout/stderr warning-stream setup, fake compiler install, compile execution, dep-trace assert, diagnostic-message assert를 공유한다. stdout/stderr warning leaf는 `69/69`줄에서 `7/7`줄로 줄었고 helper는 `98`줄이며 targeted warning-stream checks `2/2`는 green이다
  - `2026-04-28` progress: 같은 custom-compiler preview/warning helper가 이제 temp-project fixture, fake compiler script writer, generic dep-trace assert를 preview/placeholder/warning-stream lane에 함께 제공한다. `smoke/external_oracle_custom_compiler_preview_and_warnings_preview_and_stdout_preview.rs`는 `95 -> 70`줄, `smoke/external_oracle_custom_compiler_preview_and_warnings_placeholders_and_stderr_placeholders.rs`는 `92 -> 70`줄이 되었고 helper는 `151`줄이다. custom-compiler filter `11/11`, preview exact `1/1`, warning-diagnostics filter `6/6`은 green이다
  - `2026-04-29` progress: custom-compiler placeholder leaf의 fake compiler는 placeholder 확장 검증에 필요한 PDF 존재만 만들도록 shell redirection으로 output PDF를 만든다. `smoke/external_oracle_custom_compiler_preview_and_warnings_placeholders_and_stderr_placeholders.rs`는 `70 -> 63`줄이 되었고 placeholder exact check는 green이다
  - `2026-04-29` progress: custom-compiler preview leaf는 단순 mock-compiler argv를 whitespace-split token string에서 만들도록 줄였다. `smoke/external_oracle_custom_compiler_preview_and_warnings_preview_and_stdout_preview.rs`는 `70 -> 63`줄이 되었고 preview exact check는 green이다
  - `2026-04-29` progress: 같은 custom-compiler helper가 preview/placeholder/warning-stream lanes의 repeated main-only fixture compile path도 맡는다. preview leaf는 `63 -> 51`줄, placeholder leaf는 `63 -> 57`줄, helper는 `143`줄이며 affected exact lanes `4/4`는 green이다
  - `2026-04-29` progress: custom-compiler placeholder leaf도 static placeholder driver args를 whitespace-split template으로 만들고 dynamic args-log path만 별도로 push한다. placeholder leaf는 `57 -> 50`줄이며 exact lane은 green이다
  - `2026-04-23` progress: `smoke/external_oracle_pipeline_success_pdf_latex_tectonic.rs`는 이제 `crates/latexd/tests/support/external_oracle_pipeline_success_pdf_latex_tectonic_helpers.rs`를 통해 fake `tectonic` success setup, optional intro input setup, depfile rule generation, PATH isolation, compile execution, dep-trace assert, 기존 build-meta assert를 공유한다. pipeline/depfile-input/retain-toplevel leaf는 `86/91/90`줄에서 모두 `7`줄로 줄었고, tectonic smoke files는 `272`줄에서 `28`줄까지 내려갔다. helper는 `135`줄이며 broader `tectonic` smoke filter `9/9`는 green이다
  - `2026-04-23` progress: `smoke/external_oracle_pipeline_success_pdf_latex_pdflatex.rs`는 이제 `crates/latexd/tests/support/external_oracle_pipeline_success_pdf_latex_pdflatex_helpers.rs`를 통해 fake `pdflatex` fallback setup, optional intro input setup, FLS generation, PATH isolation, compile execution, dep-trace assert, main-only lane의 기존 build-meta assert를 공유한다. fallback/FLS-input/retain-toplevel leaf는 `48/86/85`줄에서 `5/7/7`줄로 줄었고, pdflatex smoke files는 `222`줄에서 `24`줄까지 내려갔다. helper는 `130`줄이며 broader `pdflatex` smoke filter `11/11`은 green이다
  - `2026-04-23` progress: `smoke/external_oracle_pipeline_success_xelatex_recorder.rs`는 이제 `crates/latexd/tests/support/external_oracle_pipeline_success_xelatex_recorder_helpers.rs`를 통해 fake `xelatex` recorder success setup, intro input setup, FLS generation, PATH isolation, compile execution, dep-trace assert, build-meta assert를 공유한다. FLS-input/retain-toplevel leaf는 `87/86`줄에서 `7/5`줄로 줄었고, xelatex recorder smoke files는 `251`줄에서 `92`줄까지 내려갔다. helper는 `104`줄이며 targeted xelatex recorder checks `2/2`는 green이다
  - `2026-04-28` progress: 같은 xelatex recorder helper가 unreadable-recorder leaf의 temp-project/build-root fixture도 공유한다. `smoke/external_oracle_pipeline_success_xelatex_recorder_unreadable_recorder.rs`는 `73 -> 50`줄, xelatex recorder smoke files는 총 `69`줄이 되었고 helper는 `127`줄이며 exact recorder checks `3/3`은 green이다
  - `2026-04-23` progress: `smoke/external_oracle_pipeline_success_latex_dvips.rs`는 이제 `crates/latexd/tests/support/external_oracle_pipeline_success_latex_dvips_helpers.rs`를 통해 fake `latex -> dvips -> ps2pdf` success setup, optional intro input setup, fake toolchain install, PATH isolation, compile execution, dep-trace assert, lane별 DVI/PS 또는 build-meta assert를 공유한다. pipeline/FLS-input/retain-toplevel leaf는 `54/82/81`줄에서 모두 `5`줄로 줄었고, latex-dvips smoke files는 `220`줄에서 `20`줄까지 내려갔다. helper는 `125`줄이며 `latex_dvips` smoke filter `3/3`은 green이다
  - `2026-04-23` progress: `smoke/external_oracle_pipeline_success_xelatex_meta.rs`는 이제 `crates/latexd/tests/support/external_oracle_pipeline_success_xelatex_meta_helpers.rs`를 통해 xelatex pipeline, build-meta-defaults, custom changed-file-order lane의 manifest/main setup, fake compiler setup, compile execution, build-meta assert를 공유한다. lane별 dep-trace check는 유지하면서 세 leaf는 `80/77/80`줄에서 `4/4/7`줄로 줄었고, xelatex/meta smoke files는 `240`줄에서 `20`줄까지 내려갔다. helper는 `213`줄이며 targeted xelatex/meta checks `3/3`은 green이다
  - `2026-04-23` progress: `smoke/external_oracle_custom_compiler_dependency_tracking.rs`는 이제 `crates/latexd/tests/support/external_oracle_custom_compiler_dependency_tracking_helpers.rs`를 통해 custom-compiler dependency-tracking용 manifest setup, optional intro input setup, fake compiler generation, compile execution, dep-trace assert, lane별 success/failure build-meta assert를 공유한다. fallback-without-FLS/depfile-preferred/FLS-input/unreadable-depfile leaf는 `62/77/72/76`줄에서 모두 `7`줄로 줄었고, dependency-tracking smoke files는 `296`줄에서 `39`줄까지 내려갔다. helper는 `219`줄이며 broader `custom_compiler` smoke filter `11/11`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_included_body_baseline_baseline_and_reversed.rs`, `smoke/unchanged_tail_noop_multi_input_between_noise_unreadable_noise.rs`, `smoke/unchanged_tail_noop_multi_input_between_noise_untracked_noise.rs`도 include-only host가 되었다. same-page late-change baseline host는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로, between-noise unchanged-tail 두 host는 `..._interleaved.rs`, `..._other_interleaved.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_multi_input_leading_between_noise_unreadable_noise.rs`, `smoke/unchanged_tail_noop_multi_input_leading_between_noise_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_between_noise_unreadable_noise.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_between_noise_untracked_noise.rs`도 include-only host가 되었다. leading-between과 mixed-between unchanged-tail 네 host는 모두 `..._interleaved.rs`, `..._other_interleaved.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_precedes_unreadable_noise.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_precedes_untracked_noise.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_precedes_unreadable_noise.rs`도 include-only host가 되었다. precedes-with-noise coverage는 모두 `..._reversed_dirty_order.rs`, `..._plain_dirty_order.rs`로 나뉘었다. targeted exact lanes `8/8`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_precedes.rs`도 include-only host가 되었다. base multi-input precedes coverage는 `..._untracked_noise.rs`, `..._unreadable_noise.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_reversed_noise_follows.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_reversed_noise_follows.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_baseline_follows_reversed_noise_follows.rs`도 include-only host가 되었다. reversed-order follows-with-noise coverage는 모두 `..._untracked_noise.rs`, `..._unreadable_noise.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_noise_follows.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_noise_follows.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_baseline_follows_noise_follows.rs`도 include-only host가 되었다. follows-with-noise coverage는 모두 `..._untracked_noise.rs`, `..._unreadable_noise.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_pipeline_success_xelatex_recorder.rs`도 include-only host가 되었다. xelatex-recorder coverage는 `..._reads_fls_inputs.rs`, `..._unreadable_recorder.rs`, `..._retains_toplevel.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_toplevel_untracked.rs`도 include-only host가 되었다. toplevel untracked trailing-input replay coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_trailing_input_baseline_unreadable.rs`도 include-only host가 되었다. same-page unreadable trailing-input replay coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_checkpoint_preference_toplevel_dirty_unreadable_noise.rs`와 `smoke/bibliography_multi_rebuild_trailing_input_root_baseline.rs`도 include-only host가 되었다. same-page checkpoint unreadable-noise coverage는 `..._follows.rs`, `..._precedes.rs`로, trailing-input root baseline coverage는 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_checkpoint_preference_included_body_unreadable.rs`와 `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_other_interleaved_variants_untracked_noise.rs`도 include-only host가 되었다. included-body checkpoint unreadable coverage와 same-page late-change other-interleaved untracked-noise coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders_baseline_follows_baseline_and_reversed.rs`, `smoke/unchanged_tail_noop_multi_input_leading_dirty_orders_baseline_follows_baseline_and_reversed.rs`, `smoke/unchanged_tail_noop_multi_input_mixed_dirty_orders_baseline_follows_baseline_and_reversed.rs`도 include-only host가 되었다. baseline/reversed unchanged-tail coverage는 모두 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_base_and_interleaved_orders_untracked_noise.rs`와 `smoke/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders_untracked_noise.rs`도 include-only host가 되었다. same-page included-body untracked-noise coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_other_interleaved_variants_unreadable_noise.rs`, `smoke/bibliography_multi_same_page_late_change_included_body_sibling_dirty_orders_unreadable_noise.rs`, `smoke/bibliography_semantically_equal_replay_later_trailing_input_unreadable.rs`도 include-only host가 되었다. unreadable-noise replay coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_trailing_input_root_extra_dirty_untracked.rs`, `smoke/bibliography_multi_rebuild_trailing_input_root_extra_dirty_unreadable.rs`, `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_base_and_interleaved_orders_unreadable_noise.rs`도 include-only host가 되었다. extra-dirty root와 same-page unreadable-noise coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_missing_artifacts_pdf_latex_toolchain.rs`도 include-only host가 되었다. pdf-latex toolchain coverage는 `..._prefers_tectonic.rs`, `..._missing_tectonic_pdf.rs`, `..._missing_pdflatex_pdf.rs`, `..._fallback_pdflatex.rs`, `..._missing_toolchain.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-29` progress: missing-artifacts output-files fixture가 pdf-latex toolchain missing-PDF/fallback lanes도 맡는다. missing tectonic/missing pdflatex/fallback leaves는 `54/53/45 -> 17/13/14`줄, helper는 `72`줄이며 affected exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_included_body_tail_noise_unreadable.rs`, `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_extra_dirty_untracked.rs`, `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_tracked_extra_dirty_unreadable.rs`도 include-only host가 되었다. included-body tail-noise unreadable coverage와 later-tracked extra-dirty coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order_untracked_noise.rs`와 `smoke/unchanged_tail_noop_ordering_multi_file_dirty_order_unreadable_noise.rs`도 include-only host가 되었다. reversed-dirty-order multi-file unchanged-tail coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order_untracked_noise.rs`와 `smoke/unchanged_tail_noop_ordering_double_leading_dirty_order_unreadable_noise.rs`도 include-only host가 되었다. reversed-dirty-order double-leading unchanged-tail coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_dirty_orders_baseline_and_reversed.rs`와 `smoke/bibliography_multi_same_page_late_change_root_baseline_baseline_and_reversed.rs`도 include-only host가 되었다. root same-page baseline/reversed bibliography coverage는 모두 `..._baseline.rs`, `..._reversed_dirty_order.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_checkpoint_preference_dirty_only_unreadable_noise.rs`, `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_unreadable_baseline_unreadable.rs`, `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_unreadable_interleaved_unreadable.rs`도 include-only host가 되었다. unreadable-noise replay coverage는 모두 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `6/6`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_unreadable_other_interleaved_unreadable.rs`도 include-only host가 되었다. unreadable other-interleaved replay coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_tail_cleanup_multi_file_other_interleaved.rs`와 `smoke/unchanged_tail_noop_ordering_double_leading_other_interleaved.rs`도 include-only host가 되었다. other-interleaved unchanged-tail coverage는 `..._untracked_between.rs`, `..._unreadable_between.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_tail_cleanup_multi_file_precedes.rs`와 `smoke/unchanged_tail_noop_tail_cleanup_multi_file_interleaved_between.rs`도 include-only host가 되었다. tail-cleanup multi-file coverage는 `..._untracked_between.rs`, `..._unreadable_between.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_included_body_sibling_interleaved_base_and_interleaved_orders_interleaved_variants.rs`도 include-only host가 되었다. included-body same-page interleaved sibling-dirty coverage는 `..._interleaved.rs`, `..._other_interleaved.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_tail_cleanup_double_leading_precedes.rs`와 `smoke/unchanged_tail_noop_tail_cleanup_double_leading_interleaved_between.rs`도 include-only host가 되었다. double-leading tail-cleanup coverage는 `..._untracked_between.rs`, `..._unreadable_between.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_tail_cleanup_double_leading_base_and_follows_baseline_and_untracked.rs`와 `smoke/unchanged_tail_noop_tail_cleanup_multi_file_base_and_follows_baseline_and_untracked.rs`도 include-only host가 되었다. baseline/follows unchanged-tail coverage는 `..._baseline.rs`, `..._untracked_follows.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_base_and_interleaved_orders_untracked_noise.rs`와 `smoke/bibliography_multi_same_page_late_change_root_sibling_dirty_orders_untracked_noise.rs`도 include-only host가 되었다. root-sibling untracked-noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_other_interleaved_variants_untracked_noise.rs`와 `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_other_interleaved_variants_unreadable_noise.rs`도 include-only host가 되었다. root-sibling other-interleaved noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_dirty_orders_unreadable_noise.rs`와 `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_base_and_interleaved_orders_unreadable_noise.rs`도 include-only host가 되었다. root-sibling unreadable-noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_ordering_mixed_leading_baseline.rs`와 `smoke/unchanged_tail_noop_ordering_mixed_leading_reversed.rs`도 include-only host가 되었다. mixed-leading unchanged-tail coverage는 `..._input.rs`, `..._toplevel.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_single_source_trailing_input_untracked_noise.rs`와 `smoke/unchanged_tail_noop_single_source_trailing_input_unreadable_noise.rs`도 include-only host가 되었다. single-source trailing-input noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_root_tail_noise_unreadable.rs`도 include-only host가 되었다. root tail unreadable-noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved_base_and_interleaved_orders_interleaved_variants.rs`도 include-only host가 되었다. root-sibling interleaved-variants coverage는 `..._interleaved.rs`, `..._other_interleaved.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_ordering_extra_dirty.rs`도 include-only host가 되었다. rev5 tracked-toplevel sibling extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty_other_interleaved.rs`도 include-only host가 되었다. rev5 tracked-toplevel sibling other-interleaved extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_toplevel_untracked_noise.rs`와 `smoke/unchanged_tail_noop_toplevel_unreadable_noise.rs`도 include-only host가 되었다. toplevel unchanged-tail noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_checkpoint_preference_toplevel_unreadable.rs`도 include-only host가 되었다. checkpoint-preference unreadable coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantically_equal_replay_later_trailing_input_toplevel_unreadable.rs`도 include-only host가 되었다. later trailing-input toplevel unreadable coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_fixpoint_reuse_included_body_tail_noise.rs`도 include-only host가 되었다. included-body tail-noise coverage는 `..._untracked.rs`, `..._unreadable.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rewrite_cases.rs`도 include-only host가 되었다. late bibliography rewrite coverage는 `..._earlier_output.rs`, `..._late_only_change.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_late_input_removal_retained_dirty_untracked.rs`도 include-only host가 되었다. late-input-removal retained-dirty untracked coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_late_input_removal_retained_dirty_unreadable.rs`도 include-only host가 되었다. late-input-removal retained-dirty unreadable coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_late_input_removal_deleted_untracked.rs`도 include-only host가 되었다. late-input-removal deleted-file untracked coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_late_input_removal_deleted_unreadable.rs`도 include-only host가 되었다. late-input-removal deleted-file unreadable coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_biblatex_print_core_and_smartcite_core_and_preamble.rs`도 include-only host가 되었다. biblatex print/smartcite coverage는 `..._core_and_smartcite_core.rs`, `..._preamble_workflow.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_biblatex_print_headings_and_supercite.rs`도 include-only host가 되었다. biblatex heading/supercite coverage는 `..._supercite.rs`, `..._bibintoc_heading.rs`, `..._bibnumbered_heading.rs`, `..._printbibheading_bibnumbered.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantic_features_output_and_include.rs`도 include-only host가 되었다. bibliography semantic output/include coverage는 `..._natexlab_output.rs`, `..._include_sections_and_labels.rs`, `..._includeonly.rs`, `..._manual_toc_starred_sections.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-29` progress: `smoke/bibliography_semantic_features_includeonly.rs`는 semantic-index fixture helper의 manifest/class setup, nested file creation, rev1 internal compile path를 재사용한다. includeonly leaf는 `68 -> 42`줄이 되었고 exact check는 green이다
  - `2026-04-17` progress: `smoke/reference_variants_theorem_setup_newtheorem.rs`도 include-only host가 되었다. theorem setup/reference coverage는 `..._defined_envs.rs`, `..._shared_counters.rs`, `..._section_scoped.rs`, `..._builtin_override_shared_scope.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_baseline_untracked_noise.rs`도 include-only host가 되었다. included-file late bibliography untracked-noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_toplevel_untracked.rs`도 include-only host가 되었다. toplevel late bibliography untracked coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_single_source_leading_input.rs`도 include-only host가 되었다. single-source leading-input unchanged-tail coverage는 `..._leading.rs`, `..._mixed.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_late_input_removal_file_remains_untracked.rs`도 include-only host가 되었다. late-input-removal file-remains untracked coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/replay_selection_late_input_removal_file_remains_unreadable.rs`도 include-only host가 되었다. late-input-removal file-remains unreadable coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_interleaved_variants.rs`도 include-only host가 되었다. rev5 input-sibling interleaved-variants coverage는 `..._base.rs`, `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_toplevel_unreadable.rs`도 include-only host가 되었다. toplevel late bibliography unreadable coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_tail_cleanup_only_dirty.rs`도 include-only host가 되었다. tail-cleanup only-dirty unchanged-tail coverage는 `..._untracked.rs`, `..._unreadable.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_input_sibling_other_interleaved_variants.rs`도 include-only host가 되었다. rev5 input-sibling other-interleaved coverage는 `..._base.rs`, `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `5/5`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_single_source_toplevel.rs`도 include-only host가 되었다. single-source toplevel unchanged-tail coverage는 `..._leading.rs`, `..._mixed.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_internal_compiler_core.rs`도 include-only host가 되었다. internal-compiler core coverage는 `..._page_metadata.rs`, `..._arxiv_basic.rs`, `..._grouped_usepackage.rs`, `..._hyperxmp.rs`로 나뉘었다. targeted exact lanes는 `3/4`만 green이고, `bundled_arxiv_basic_fixture_builds_with_internal_compiler`는 이미 dirty 상태인 `fixtures/arxiv-basic/main.tex` 때문에 copied fixture가 기대 `3` page 대신 `1` page만 렌더링되어 block 상태다
  - `2026-04-25` progress: `smoke/external_oracle_internal_compiler_core.rs`는 이제 `crates/latexd/tests/support/external_oracle_internal_compiler_core_helpers.rs`를 통해 internal-compiler manifest/class setup, compile path, first-page artifact URL assert를 공유한다. page-metadata leaf는 `117 -> 96`줄, arxiv-basic은 `66 -> 50`줄, grouped-usepackage는 `48 -> 34`줄, hyperxmp는 `52 -> 38`줄이 되었고 helper는 `61`줄이다. non-arxiv targeted exact lanes `3/3`은 green이며 arxiv lane은 기존 dirty fixture page-count drift 때문에 계속 blocked다
  - `2026-04-25` progress: 같은 internal-compiler core helper가 이제 single-page artifact/syncmap와 dep-trace assertion도 담당한다. page-metadata leaf는 `96 -> 40`줄로 더 줄었고 grouped-usepackage/hyperxmp leaf도 같은 artifact 검증을 재사용하며 helper는 `122`줄이다. non-arxiv exact lanes `3/3`은 계속 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_year_aliases.rs`도 include-only host가 되었다. year/alias bibliography coverage는 `..._citeyearpar.rs`, `..._natexlab.rs`, `..._citefullauthor.rs`, `..._alias_variants.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_internal_compiler_compat.rs`도 include-only host가 되었다. internal-compiler compatibility coverage는 `..._revtex.rs`, `..._cleveref.rs`, `..._minted.rs`, `..._fontspec.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-29` progress: `smoke/external_oracle_internal_compiler_compat.rs`는 이제 `crates/latexd/tests/support/external_oracle_internal_compiler_compat_helpers.rs`의 compat fixture/output helper를 공유한다. revtex/cleveref/minted/fontspec leaves는 `57/52/53/48 -> 28/25/22/22`줄, helper는 `63`줄이며 exact compat checks `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_pipeline_success_xelatex_meta.rs`도 include-only host가 되었다. xelatex/meta success coverage는 `..._pipeline.rs`, `..._build_meta.rs`, `..._changed_file_order.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_pipeline_success_pdf_latex_pdflatex.rs`도 include-only host가 되었다. pdflatex-fallback pdf-latex success coverage는 `..._fallback.rs`, `..._fls_inputs.rs`, `..._retains_toplevel.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/external_oracle_pipeline_success_pdf_latex_tectonic_support_and_depfile.rs`도 include-only host가 되었다. tectonic pdf-latex success coverage는 `..._pipeline.rs`, `..._depfile.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_missing_artifacts_ps2pdf_stage.rs`도 include-only host가 되었다. ps2pdf-stage failure coverage는 `..._stderr.rs`, `..._stdout.rs`, `..._quiet.rs`, `..._spawn.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-29` progress: `smoke/external_oracle_missing_artifacts_ps2pdf_stage.rs`도 같은 stage-helper 형태를 `crates/latexd/tests/support/external_oracle_missing_artifacts_ps2pdf_stage_helpers.rs`로 적용했다. stderr/stdout/quiet/spawn leaves는 `56/56/55/52 -> 19/19/18/15`줄, helper는 `53`줄이며 exact ps2pdf-stage checks `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_missing_artifacts_latex_stage.rs`도 include-only host가 되었다. latex-stage failure coverage는 `..._stderr.rs`, `..._stdout.rs`, `..._quiet.rs`, `..._spawn.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-29` progress: `smoke/external_oracle_missing_artifacts_latex_stage.rs`는 이제 `crates/latexd/tests/support/external_oracle_missing_artifacts_latex_stage_helpers.rs`의 latex-stage failure fixture/assert helper를 공유한다. stderr/stdout/quiet/spawn leaves는 `56/56/55/52 -> 19/19/18/15`줄, helper는 `53`줄이며 exact latex-stage checks `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_missing_artifacts_dvips_stage.rs`도 include-only host가 되었다. dvips-stage failure coverage는 `..._spawn.rs`, `..._quiet.rs`, `..._stderr.rs`, `..._stdout.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-29` progress: `smoke/external_oracle_missing_artifacts_dvips_stage.rs`도 같은 stage-helper 형태를 `crates/latexd/tests/support/external_oracle_missing_artifacts_dvips_stage_helpers.rs`로 적용했다. stderr/stdout/quiet/spawn leaves는 `56/56/55/52 -> 19/19/18/15`줄, helper는 `53`줄이며 exact dvips-stage checks `4/4`는 green이다
  - `2026-04-17` progress: `smoke/external_oracle_pipeline_success_latex_dvips.rs`도 include-only host가 되었다. latex-dvips-ps2pdf success coverage는 `..._pipeline.rs`, `..._fls_inputs.rs`, `..._retains_toplevel.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_late_bibliography_rebuild_included_file_baseline_unreadable_noise.rs`도 include-only host가 되었다. included-file late-bibliography unreadable-noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_fixpoint_reuse_toplevel_tail_noise.rs`도 include-only host가 되었다. toplevel fixpoint-reuse tail-noise coverage는 `..._untracked.rs`, `..._unreadable.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_semantic_features_backdate_and_rebuild.rs`도 include-only host가 되었다. semantic-feature backdate/rebuild coverage는 `..._backdate.rs`, `..._rebuild.rs`로 나뉘었다. targeted exact lanes `2/2`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_citation_fields_identifiers_identifier_and_date.rs`도 include-only host가 되었다. citation-field identifier/date coverage는 `..._citefield.rs`, `..._identifiers.rs`, `..._date.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_citation_fields_identifiers_url_and_number.rs`도 include-only host가 되었다. citation-field URL/number coverage는 `..._citeurl.rs`, `..._bibfield_url.rs`, `..._citenum.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_natbib_variants_textual.rs`도 include-only host가 되었다. textual natbib coverage는 `..._basic.rs`, `..._capitalized.rs`, `..._notes.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_natbib_variants_other_variants.rs`도 include-only host가 되었다. remaining natbib coverage는 `..._citetext.rs`, `..._starred.rs`, `..._parenthetical.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/bibliography_features_citation_fields_metadata.rs`도 include-only host가 되었다. citation-field metadata coverage는 `..._bibinfo.rs`, `..._generic.rs`, `..._bibfield.rs`로 나뉘었다. targeted exact lanes `3/3`은 green이다
  - `2026-04-17` progress: `smoke/reference_variants_page_ranges.rs`도 include-only host가 되었다. page-range reference coverage는 `..._page_oriented.rs`, `..._pagerefrange.rs`, `..._varioref.rs`, `..._cpagerefrange.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/reference_variants_cleveref_forms.rs`도 include-only host가 되었다. wrapper/cleveref command coverage는 `..._wrappers.rs`, `..._cref.rs`, `..._namecref.rs`, `..._crefrange.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/reference_variants_command_forms.rs`도 include-only host가 되었다. theorem/fullref/label-only command coverage는 `..._thmref.rs`, `..._fullref.rs`, `..._plural_namecref.rs`, `..._labelcref.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/reference_variants_autoref.rs`도 include-only host가 되었다. autoref coverage는 `..._base.rs`, `..._chapter_appendix.rs`, `..._subsection_depth.rs`, `..._paragraph_depth.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty_interleaved.rs`도 include-only host가 되었다. rev5 tracked-toplevel sibling interleaved extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/unchanged_tail_noop_toplevel_untracked_noise.rs`와 `smoke/unchanged_tail_noop_toplevel_unreadable_noise.rs`도 include-only host가 되었다. toplevel unchanged-tail noise coverage는 `..._follows.rs`, `..._precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-17` progress: `smoke/bibliography_multi_rebuild_rev5_tracked_toplevel_sibling_extra_dirty_interleaved.rs`도 include-only host가 되었다. rev5 tracked-toplevel sibling interleaved extra-dirty coverage는 `..._untracked_follows.rs`, `..._untracked_precedes.rs`, `..._unreadable_follows.rs`, `..._unreadable_precedes.rs`로 나뉘었다. targeted exact lanes `4/4`는 green이다
  - `2026-04-13` progress: `smoke/external_oracle_pipeline_success.rs`도 host file이 되었고, successful external-oracle matrix는 `..._latex_dvips` + `..._xelatex_and_meta` + `..._pdf_latex`로 다시 나뉘어 engine family별 success path와 meta lane이 분리됐다. targeted success-path engine/meta checks는 green이다
  - `2026-04-13` progress: `smoke/bibliography_multi_same_page_late_change_root_sibling_interleaved.rs`도 host file이 되었고, same-page late-semantic root-body bibliography matrix는 `..._base_and_interleaved_orders` + `..._other_interleaved_variants`로 다시 나뉘어 base/interleaved branch와 other-interleaved branch가 분리됐다. targeted root-body interleaved filters는 green이다
  - `2026-04-13` progress: `smoke/external_oracle_custom_compiler.rs`도 host file이 되었고, custom-compiler matrix는 `..._preview_and_warnings` + `..._failures` + `..._dependency_tracking` + `..._pipeline_warnings`로 다시 나뉘어 preview/warning lane, failure lane, dep-trace lane, pipeline-warning lane이 분리됐다. targeted mock/custom/warning checks는 green이다
  - `2026-04-13` progress: `smoke/unchanged_tail_noop_single_source.rs`도 host file이 되었고, single-source unchanged-tail matrix는 `..._trailing_input` + `..._leading_input` + `..._toplevel`로 다시 나뉘어 trailing-input branch, leading-input branch, toplevel branch가 분리됐다. targeted trailing/leading/toplevel checks는 green이다
  - `2026-04-13` progress: `smoke/unchanged_tail_noop_multi_input_dirty_orders.rs`도 host file이 되었고, multi-input dirty-order matrix는 `..._baseline_follows` + `..._precedes`로 다시 나뉘어 baseline/follows branch와 precedes branch가 분리됐다. targeted exact/plain-follow checks와 broader reversed-order filter는 green이다
  - `2026-04-13` progress: `smoke/external_oracle_engine_failures.rs`도 host file이 되었고, engine-failure matrix는 `..._xelatex` + `..._warning_and_spawn` + `..._tectonic` + `..._pdflatex`로 다시 나뉘어 xelatex family, warning/spawn lane, tectonic failure lane, pdflatex failure lane이 분리됐다. targeted engine-family failure/warning checks는 green이다
  - `2026-04-13` progress: `smoke/bibliography_output.rs`도 host file이 되었고, bibliography-output normalization matrix는 `..._basic_wrappers` + `..._punctuation_and_spacing` + `..._textstyle_and_starred`로 다시 나뉘어 기본 wrapper lane, punctuation/spacing helper lane, textstyle/starred/name-affix lane이 분리됐다. targeted bibliography-output wrapper filter는 green이다
  - `2026-04-13` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_baseline.rs`도 host file이 되었고, included-body same-page trailing-input baseline matrix는 `..._baseline_reversed_and_follows` + `..._precedes_and_unreadable`로 다시 나뉘어 baseline/reversed/follows branch와 precedes/unreadable branch가 분리됐다. targeted replay/fallback exact lanes는 green이다
  - `2026-04-14` progress: `smoke/bibliography_multi_same_page_trailing_input_baseline.rs`도 host file이 되었고, same-page trailing-input baseline matrix는 `..._reversed`, `..._untracked`, `..._unreadable`로 다시 나뉘었다. targeted replay/fallback exact lanes `6/6`은 green이다
  - `2026-04-14` progress: `smoke/bibliography_multi_same_page_trailing_input_sibling_extra_dirty_untracked.rs`도 host file이 되었고, top-level same-page trailing-input extra-dirty untracked-noise matrix는 `..._dirty_orders`, `..._interleaved_orders`, `..._other_interleaved_orders`로 다시 나뉘었다. targeted replay/fallback exact lanes `6/6`은 green이다
  - `2026-04-14` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_unreadable.rs`도 host file이 되었고, included-body same-page trailing-input extra-dirty unreadable-noise matrix는 `..._dirty_orders`, `..._interleaved_orders`, `..._other_interleaved_orders`로 다시 나뉘었다. targeted replay/fallback exact lanes `6/6`은 green이다
  - `2026-04-14` progress: `smoke/bibliography_multi_same_page_trailing_input_included_body_sibling_extra_dirty_untracked.rs`도 host file이 되었고, included-body same-page trailing-input extra-dirty untracked-noise matrix는 `..._dirty_orders`, `..._interleaved_orders`, `..._other_interleaved_orders`로 다시 나뉘었다. targeted replay/fallback exact lanes `6/6`은 green이다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel.rs`도 2-line host file이 되었고, 내용은 `unchanged_tail_noop_with_toplevel_trailing` + `unchanged_tail_noop_with_toplevel_leading`으로 나뉘었다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading.rs`도 host file이 되었고, trailing branch는 `..._trailing_ordering` + `..._trailing_noise` + `..._trailing_replay`, leading branch는 `..._leading_ordering` + `..._leading_noise`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_ordering.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading_ordering.rs`도 host file이 되었고, ordering-only cases는 `..._baseline` + `..._interleaved`로 더 잘렸다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_leading_noise.rs`도 host file이 되었고, leading noise matrix는 `..._leading_noise_baseline` + `..._leading_noise_interleaved` + `..._leading_noise_other_interleaved`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_pre.rs`와 `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_post.rs`도 host file이 되었고, trailing noise matrix는 `..._trailing_noise_baseline` + `..._trailing_noise_between` + `..._trailing_noise_interleaved` + `..._trailing_noise_other_interleaved`로 다시 나뉘었다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_baseline.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading_noise_baseline.rs`도 host file이 되었고, baseline-order noise cases는 각각 `..._reversed` + `..._plain`으로 더 잘렸다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_interleaved.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading_noise_interleaved.rs`도 host file이 되었고, interleaved noise cases는 각각 `..._follows_interleaved` + `..._precedes_interleaved`로 더 잘렸다. `between_them` outlier는 `smoke/unchanged_tail_noop_with_toplevel_trailing_replay.rs`에만 남겨 두었고, broad `unchanged_tail` smoke는 `238/238` green이다
  - `2026-04-25` progress: replay-only `smoke/unchanged_tail_noop_with_toplevel_trailing_replay.rs` outlier도 기존 trailing-noise fixture/rewrite/untracked marker/second-pass compile helper를 재사용한다. leaf는 `116 -> 60`줄로 줄었고 targeted exact replay lane은 green이다
  - `2026-04-29` progress: 같은 replay-only outlier는 expected replace/insert page-patch sequence를 하나의 slice pattern으로 검증한다. leaf는 `60 -> 46`줄이며 exact replay lane은 green이다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_other_interleaved.rs`와 `smoke/unchanged_tail_noop_with_toplevel_leading_noise_other_interleaved.rs`도 host file이 되었고, other-interleaved noise cases는 각각 `..._follows_other_interleaved` + `..._precedes_other_interleaved`로 더 잘렸다. broad `unchanged_tail` smoke는 계속 `238/238` green이다
  - `2026-04-09` progress: `smoke/unchanged_tail_noop_with_toplevel_trailing_noise_between.rs`도 host file이 되었고, `between` outlier는 `..._between_untracked` + `..._between_unreadable`로 더 잘렸다. broad `unchanged_tail` smoke는 계속 `238/238` green이다
  - `2026-04-10` progress: `smoke/replay_selection_input_boundaries_toplevel_exit.rs`도 host file이 되었고, top-level input-exit replay matrix는 `replay_selection_input_boundaries_toplevel_exit_baseline` + `replay_selection_input_boundaries_toplevel_exit_multi_file` + `replay_selection_input_boundaries_toplevel_exit_clamp`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries_repeated_include_baseline.rs`도 host file이 되었고, repeated-include contract matrix는 `replay_selection_include_boundaries_repeated_include_baseline_same_file` + `replay_selection_include_boundaries_repeated_include_baseline_multi_file`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/replay_selection_input_boundaries_toplevel_exit_multi_file.rs`도 host file이 되었고, multi-file top-level input-exit replay cases는 `replay_selection_input_boundaries_toplevel_exit_multi_file_appendix_first` + `replay_selection_input_boundaries_toplevel_exit_multi_file_main_first`로 다시 나뉘었다
  - `2026-04-22` progress: `smoke/replay_selection_input_boundaries_toplevel_exit_multi_file.rs`는 이제 `crates/latexd/tests/support/replay_selection_input_boundaries_toplevel_exit_multi_file_helpers.rs`를 통해 multi-file top-level input-exit replay fixture, rev2 rewrite, checkpoint-selection/build-meta assert를 공유한다. appendix-first와 main-first leaf는 `19/20`줄만 남았고, host는 `4`줄, helper는 `157`줄이며 targeted exact lanes `2/2`는 green이다
  - `2026-04-10` progress: `smoke/replay_selection_include_boundaries_nested_exit_core_toplevel.rs`도 host file이 되었고, nested-exit vs late-toplevel competition cases는 `replay_selection_include_boundaries_nested_exit_core_toplevel_plain` + `replay_selection_include_boundaries_nested_exit_core_toplevel_appendix`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/replay_selection_late_input_removal_deleted.rs`도 host file이 되었고, deleted-input rebuild matrix는 `replay_selection_late_input_removal_deleted_baseline` + `replay_selection_late_input_removal_deleted_untracked` + `replay_selection_late_input_removal_deleted_unreadable`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/replay_selection_late_input_removal_retained_dirty.rs`도 host file이 되었고, retained-file-dirty rebuild matrix는 `replay_selection_late_input_removal_retained_dirty_baseline` + `replay_selection_late_input_removal_retained_dirty_untracked` + `replay_selection_late_input_removal_retained_dirty_unreadable`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/replay_selection_late_input_removal_file_remains.rs`도 host file이 되었고, file-remains rebuild matrix는 `replay_selection_late_input_removal_file_remains_baseline` + `replay_selection_late_input_removal_file_remains_untracked` + `replay_selection_late_input_removal_file_remains_unreadable`로 다시 나뉘었다
  - `2026-04-10` progress: `smoke/reference_variants.rs`도 host reduction을 시작했고, 첫 contiguous basic-reference cluster는 `smoke/reference_variants_basic_refs.rs`로 빠져 `eqref` / `subref` / `nameref` / `titleref` smoke와 두 `nameref` title-preference regressions를 큰 `autoref` / theorem / appendix numbering matrix에서 분리했다
  - `2026-04-10` progress: `smoke/reference_variants.rs`는 이미 한 단계 더 내려가서 contiguous `autoref` depth/appendix block은 `smoke/reference_variants_autoref.rs`, 그 다음 equation/float/list/caption block은 `smoke/reference_variants_autoref_equations_floats.rs`로 빠졌고, host는 이제 `algorithm` / theorem / appendix numbering cluster에서 다시 시작한다
  - `2026-04-10` progress: 그 다음 contiguous theorem-setup cluster도 `smoke/reference_variants_theorem_setup.rs`로 빠져 `algorithm` / theorem / `newtheorem` / theoremstyle / proof-header smoke를 host 밖으로 내렸고, 남은 host는 이제 `claim` / `example` / theorem-kind variants와 later appendix-numbering block에서 다시 시작한다
  - `2026-04-10` progress: `claim` / `example` / theorem-kind reference variants block도 `smoke/reference_variants_theorem_kinds.rs`로 빠졌고, 남은 host는 이제 `thmref` / `fullref` / cleveref-style command variants와 later numbering block에서 다시 시작한다
  - `2026-04-10` progress: `thmref` / `fullref` / plural `namecref` / `labelcref` command-form reference variants block도 `smoke/reference_variants_command_forms.rs`로 빠졌고, 남은 host는 이제 wrapper stripping과 `cleveref` / page-range / numbering block에서 다시 시작한다
  - `2026-04-10` progress: wrapper stripping + `cleveref` / `namecref` / `crefrange` command variants block도 `smoke/reference_variants_cleveref_forms.rs`로 빠졌고, 남은 host는 이제 page-oriented refs, page/range refs, multi-label cleveref, and numbering block에서 다시 시작한다
  - `2026-04-10` progress: page-oriented refs + page/range reference variants block도 `smoke/reference_variants_page_ranges.rs`로 빠졌고, 남은 host는 이제 multi-label cleveref와 numbering/appendix block에서 다시 시작한다
  - `2026-04-29` progress: page-range reference leaves가 기존 reference-variant internal fixture/compile path를 재사용한다. page-oriented/pagerefrange/varioref/cpagerefrange leaves는 local setup을 내려놓고 `55/46/49/47 -> 30/21/24/22`줄이 되었으며 exact lanes `4/4`는 green이다
  - `2026-04-10` progress: multi-label cleveref pluralization/list variants block도 `smoke/reference_variants_multi_label_cleveref.rs`로 빠졌고, 남은 host는 이제 TOC/label numbering과 appendix-numbering block만 직접 들고 있다
  - `2026-04-10` progress: `smoke/reference_variants.rs`는 이제 include-only host가 됐다. 남은 TOC/heading numbering block은 `smoke/reference_variants_toc_numbering.rs`, appendix numbering block은 `smoke/reference_variants_appendix_numbering.rs`로 빠졌고 targeted numbering checks와 `reference_variants` family filter가 green이다
  - next `CH-5` step: include-only가 된 `reference_variants`, `external_oracle`, `external_oracle_missing_artifacts`, `external_oracle_custom_compiler`, `external_oracle_engine_failures`, `bibliography_output`, `bibliography_features`, `bibliography_checkpoint_preference`, `bibliography_semantically_equal_replay`, `bibliography_multi_replay_checkpoint_preference`, `bibliography_multi_replay_trailing_input`, `bibliography_multi_same_page_checkpoint_preference`, unchanged-tail ordering/toplevel/multi-input shards, same-page late-change root/included-body split, single-source split, same-page trailing-input baseline split, top-level/included-body trailing-input extra-dirty untracked split, included-body trailing-input extra-dirty unreadable split, checkpoint-preference top-level/input split, citation-fields split, multi-file tail-cleanup split, double-leading tail-cleanup split, mixed-toplevel dirty-order split, leading-toplevel baseline/follows leaf split, mutation split, later-trailing-input replay split, unreadable rebuild interleaved-leaf split, top-level later-trailing-input replay split, multi-input baseline/follows leaf split, same-page late-change dirty/interleaved leaf split, same-page trailing-input unreadable leaf split, fixpoint-reuse split, rev5/trailing-toplevel interleaved split, leading-toplevel interleaved leaf split, and other-interleaved unchanged-tail leaf split 상태를 유지하면서, 남은 oversized smoke shards 중 `bibliography_features_biblatex_print_preamble_workflow.rs`, `bibliography_multi_rebuild_rev5_baseline.rs`, `unchanged_tail_mutations_shifted_and_deleted_shifted.rs` 같은 다음 large leaf를 고르거나 commit boundary를 정리하기

## Milestone Split

### Completed

- `M11.1`: concrete semantic aux artifact
- `M11.2`: semantic equality, backdating, rerun-to-fixpoint
- `M11.3`: common natbib/biblatex/theorem/float/reference rewrite surface
- `M11.4`: checkpoint-seeded replay, observability, page/checkpoint reuse
- `M12.1`: wrapper-heavy corpus baseline
- `M12.2`: bibliography/toolchain realism
- `M12.3`: package/wrapper interaction + semantic artifact tightening
- `M12.4`: failure/recovery + structured artifact coverage
- `M12.5`: renderer/session hardening
- `M12.6`: sync/editor hardening

### Current Backlog Meaning

- 이 문서는 더 이상 `M12` 미완료 항목을 추적하지 않는다.
- 아래 backlog는 모두 post-`M12` follow-on 또는 future scope다.

## A. Compatibility / Hardening Backlog

### A1. Corpus Realism

- `Done`: 더 큰 multi-file preamble fixture 추가
  - local `cls/sty/cfg/def`가 여러 단계로 엮인 프로젝트
  - preamble file split + nested `\input` / `\include`
  - wrapper class/package 체인이 길어진 실제 문서형 fixture
- `Done`: 더 현실적인 bibliography workflow fixture 추가
  - multi-`.bbl` + heading-only bibliography + manual title rewrite가 함께 있는 문서
  - bibliography asset delete/recover뿐 아니라 aux artifact drift가 같이 있는 문서
  - style/order change + semantic-change + recovery가 한 fixture 안에 함께 있는 문서
- `Done`: 더 현실적인 theorem/appendix/float/reference 조합 fixture 추가
  - theorem + cleveref + appendix + float captions가 한 문서에 섞인 경우
  - short title + manual TOC + theorem/block title refs가 함께 있는 경우
- `Done`: 실제 arXiv-style project shapes 확대
  - 큰 root README/profile 조합
  - 여러 local package/class와 split document가 같이 있는 문서
  - option-heavy wrapper stack과 semantic aux-heavy body가 동시에 있는 문서
- `Future`: fixture를 더 실제 논문 묶음으로 확장
  - semantic-aux-heavy paper보다 더 큰 “paper family” fixture
  - 분야별 archetype fixture
  - recent larger additions now include `split-preamble-heading-theorem-paper-family-workflow`, `split-preamble-bibliography-heavy-paper-family-workflow`, `split-preamble-appendix-heavy-paper-family-workflow`, `split-preamble-package-wrapper-paper-family-workflow`, `split-preamble-appendix-reference-paper-family-workflow`, `split-preamble-manual-heading-reference-paper-family-workflow`, `split-preamble-citation-caption-reference-heavy-paper-family-workflow`, `split-preamble-biblatex-heading-reference-heavy-paper-family-workflow`, `split-preamble-manual-heading-caption-reference-heavy-paper-family-workflow`, and `split-preamble-varioref-reference-heavy-paper-family-workflow`
    - theorem-heavy paper
    - bibliography-heavy paper
    - appendix-heavy paper
    - package-wrapper-heavy paper
    - appendix-reference paper
    - manual-heading-reference paper
    - citation-caption-reference-heavy paper
    - biblatex-heading-reference-heavy paper
    - manual-heading-caption-reference-heavy paper
    - varioref-reference-heavy paper
    - bibliography-toolversion paper
    - package-interaction paper
    - failure-semantic-chain paper

### A2. Toolchain / Package Compatibility

- `Done`: package-specific reference semantics 확대
  - 현재 hard-coded surface를 넘어 package interaction regression 추가
  - `hyperref`, `cleveref`, `varioref`, theorem/appendix/float surfaces의 혼합 경로 확대
- `Done`: bibliography/tool-version semantics 심화
  - `semantic.aux` / `build-meta.json`에서 style/input metadata drift를 더 직접적으로 검증
  - bibliography tool-version 차이를 나타내는 artifact-level fixture 정리
- `Done`: macro/package-driven semantic discovery gap 축소
  - 현재 hard-coded command list 바깥의 common wrapper 패턴을 fixture로 먼저 포착
  - custom theorem / wrapper package / wrapper class가 semantic discovery에 미치는 영향 확대
- `Future`: concrete aux/toolchain artifact 경로 심화
  - bibliography style/input change와 semantic equality/backdating 관계를 더 엄격하게 모델링
  - package-specific semantic artifacts를 `semantic.aux` 또는 companion artifact로 노출할지 검토
- `Future`: wider engine-profile hardening
  - 현재 narrow `pdflatex` path 위주의 shim에서 더 실제적인 local toolchain profile로 확장
  - `xelatex` / `latex+dvips_ps2pdf` 쪽 narrow fixture 확대

### A3. Failure / Recovery / Stability

- `Done`: failure/recovery corpus 조합 확대
  - missing input/package/class/font/cache 외에 bibliography + semantic stack 혼합 recovery
  - failure 뒤 later semantic-change rebuild까지 이어지는 revision chain
- `Done`: structured artifact expectations 확대
  - `build-meta.json`, `semantic.aux`, `semantic-index.json`에서 중요한 fields를 corpus에서 더 직접적으로 고정
- `Future`: corpus diagnostics expectations 정교화
  - expected failure message뿐 아니라 failure kind/classification까지 고정
  - recovery revision에서 reused/rebuilt page metadata 변화까지 고정

## B. Renderer / Preview Hardening

### B1. Long-Lived Renderer Session

- `Done`: Phase 1 actor/session wrapper
  - same document root + renderer mode requests now share one render lane
  - current full-page cold miss path no longer constructs a fresh renderer path per miss
- `Done`: [`renderer-session-plan.md`](/home/seorii/dev/hancomac/latexd/docs/renderer-session-plan.md) Phase 1
- `Done`: Phase 2
  - basic attach/detach, attached-page lookup, attached revision metadata-set lookup, attached revision debug exposure, and actor-owned LRU + page-budget retention are in
  - broader revision lifecycle ownership is now mostly about longer mixed old/new incremental chains rather than fixed build-loop detach policy
  - revision attachment / detach
  - unchanged pages reused from older revision도 session에서 바로 render 가능하게 만들기
- `Done`: Phase 5
  - actor spawn/restart/attach/detach/evict/full-render/tile-render/prewarm/fallback counters, debug surface, recent revision/page keyed event ring, matching structured debug logs, coarse per-event durations, aggregate latency summaries, and attached-session budget context are in
  - renderer crash 시 cache/fallback degradation path와 longer-session observability를 더 넓게 다듬기
- `Done`: Phase 3
  - rectangle-request tile path has started in a narrow form
  - same tile request는 actor-owned `render_tiles`와 small rect-tile cache를 타고, mixed cached+uncached batch도 missing rect만 다시 렌더한다
  - rect-tile cache는 이제 global LRU budget 위에 per-page rect budget도 같이 가져서 one page의 rectangle churn이 다른 page tile ownership을 밀어내지 않게 됐다
  - tile cache eviction도 이제 debug surface에서 counter/event로 보여서 per-page/global rect ownership churn을 직접 추적할 수 있다
  - repeated identical tile hit는 actor debug surface에서 tile-cache count, concrete cached rect entry list, explicit `ReuseTiles` event로 구분해 볼 수 있다
  - broader rectangle batching/display-list ownership은 여전히 남아 있다
- `Done`: Phase 4
  - session-aware prewarm
  - viewport prewarm의 narrow actor-owned warm path, same revision/page/zoom inflight duplicate suppression, current-page neighboring zoom-step warmup은 들어갔고, actor-owned warm bucket retention/debug exposure도 이제 들어왔다
  - warm bucket은 이제 per-page zoom-bucket budget도 가져서 single-page zoom churn이 다른 warmed page state를 밀어내지 않게 됐다
  - 남은 건 longer-lived warm ownership tuning이다

### B2. Preview / Sync Fidelity

- `Done`: sync geometry fidelity 확대
  - 현재 line-aware sync box를 더 안정적인 box-level geometry로 확장
  - page-local source/output range drift regression과 item-level output window validation은 들어갔고, 남은 건 true box attribution에 더 가까운 geometry tightening이다
- `Done`: editor bridge 강화
  - source-jump/open-source bridge response가 nearest page/item/page-local source-output window와 page geometry, ready-to-use canonical source hash를 포함하게 됐고, viewer도 그 canonical hash를 current page/selected source, source link, URL history, resolved embedding event(`latexd:source-jump-resolved`, `latexd:source-hover-resolved`, `latexd:open-source-resolved`)로 소비하게 됐다
  - canonical `source_hash`도 이제 column이 있으면 `&column=`까지 유지하고, viewer request reconstruction도 그 canonical column을 다시 읽어서 source pane / local editor / preview roundtrip에서 line-only ambiguity를 줄인다
  - `window.latexdJumpToSource(...)` / `window.latexdSelectSource(...)` / `window.latexdHoverSource(...)`도 이제 `{ sourceHash }` / `{ source_hash }` 뿐 아니라 plain `"#src=..."` 입력을 직접 풀 수 있고, `window.latexdOpenSelectedSource(...)` / `window.latexdPreviewSelectedSource(...)`도 same canonical input을 직접 POST할 수 있다. `latexdOpenSelectedSource(...)`는 direct canonical input과 `launch: false`도 같은 imperative entrypoint에서 같이 받을 수 있다. `/api/source-jump/<rev>` 와 `/api/open-source/<rev>` 둘 다 `source_hash`-only request를 받아 nested-path/column selection을 한 번 더 roundtrip해도 재구성 오차를 줄이고, direct hash/object path detail도 resolved/failed event와 returned promise에서 같은 canonical `source` shape를 다시 내보낸다
  - sync item은 stable `item_id`를 같이 싣고, viewer는 syncmap refresh에서 같은 `item_id`가 남아 있으면 selection/hover를 새 geometry/output window로 다시 붙인다
  - `/api/source-jump/<rev>`와 `/api/open-source/<rev>`가 이제 둘 다 absolute file path, editor cwd, launch-support state, `file_uri`, `editor_uri`, materialized `editor_program` / `editor_args` / `editor_command_line` preview, `editor_preview_kind`(`none` / `uri` / `command` / `command_and_uri`)를 같은 shape로 돌려서 external shell/local editor handoff를 jump/open-source 어느 path에서도 같은 방식으로 제어할 수 있게 됐다
  - imperative `window.latexdJumpToSource(...)`, `window.latexdSelectSource(...)`, `window.latexdHoverSource(...)`도 이제 raw response 대신 same resolved detail payload를 promise로 돌려줘서, embedding shell이 event listener 없이도 canonical hash / page-item context / editor preview를 바로 재사용할 수 있다
  - `/api/open-source/<rev>` launch placeholder도 `rev/source_hash/page/item/output` context와 `{item_id}`, `{editor_cwd}`, `{absolute_file}`뿐 아니라 `{editor_program}`, `{editor_command_line}`, `{editor_preview_kind}`까지 직접 materialize 하게 됐고, `launch: false` preview-only path나 no-bridge normal request에서도 spawn 없이 resolved handoff payload를 먼저 받을 수 있게 됐다. built-in viewer의 explicit open-source action도 이제 `editorBridgeEnabled=false`에서 same resolved payload를 실제 fetch해서 `latexd:open-source-resolved`로 넘기고, `source-jump` / `source-hover` / `open-source` fetch failure는 `latexd:*‑failed` event로 분리해서 embedding shell이 preview fallback과 request failure를 구분할 수 있다
  - 남은 건 현재 DOM/hash/local command bridge를 넘는 stronger external editor integration이다
  - source pane / local editor / preview selection roundtrip 정교화

## C. Semantic / Compiler Refinement

### C1. Replay / Invalidation Tightening

- `Done`: semantic-changing bibliography input no longer reuses stale preamble prefixes
  - changed `.bbl`이 current bibliography set에 포함되고 semantic aux가 실제로 달라지면 final semantic build는 reused preamble checkpoint 대신 base snapshot에서 다시 돈다
  - semantically-equal later `.bbl` edits는 그대로 page/checkpoint reuse를 유지하고, semantic-changing `.bbl` edits만 conservative rebuild로 내려간다
- `Future`: semantic artifact 중심 invalidation 강화
  - raw source/executed source heuristic보다 semantic artifact drift를 더 직접 기준으로 삼기
  - backdating / rebuild / replay 선택을 더 구조화하기
- `Future`: semantic change와 page reuse 관계 정교화
  - currently rebuilt page count가 conservative한 경로 줄이기
  - semantic-equal revision에서 page/checkpoint reuse를 더 공격적으로 유지하기
- `Done`: larger paper-family recovery metadata drift 고정
  - `split-preamble-*paper-family-workflow` recovery rev가 `rebuilt_page_count` / `reused_page_count` / `semantic_pass_count`까지 structured JSON expectation으로 잠겨 있다

### C2. Observability

- `Done`: `build-meta.json` / `semantic-index.json` / `semantic.aux`와 `FAIL-JSON-EXPECT.txt`를 기준으로 실제 운영 판단에 필요한 field 정리
- `Done`: representative larger bibliography/paper-family fixture의 `semantic-index.json` root/file-summary drift 고정
  - `split-preamble-bibliography-workflow` / `split-preamble-paper-family-workflow`가 base shape + bibliography input-order/style drift를 structured JSON으로 직접 핀다
  - `split-preamble-biblatex-paper-family-workflow` / `split-preamble-manual-biblatex-paper-family-workflow`가 base shape + bibliography input-order/manual-heading drift를 structured JSON으로 직접 핀다
- `Done`: current failure corpus now has `FAIL-JSON-EXPECT.txt` coverage end-to-end
  - split-preamble realistic workflow family와 optioned/diagnostic failure fixtures까지 preamble/package/class/body/command/error failure chain의 structured failure classification이 corpus-level default가 됐다
- `Future`: corpus gate에서 structured JSON/artifact expectation coverage 확대
- `Future`: lightweight dashboard/debug view
  - revision별 semantic pass, page reuse, checkpoint start, dirty files를 쉽게 볼 수 있는 surface

## D. Future Goals

아래 항목들은 지금 당장 `M12` 호환 목표는 아니지만, 추후 목표로 적어 둔다.

### D1. Fuller TeX / Toolchain Compatibility

- `Future`: source-rewrite scaffold를 넘는 richer aux/toolchain pipeline
- `Future`: BibTeX/Biber process emulation 심화
- `Future`: macro-generated semantic discovery 확대
- `Future`: wider TeX Live/package compatibility
- `Future`: full `xelatex` / `latex+dvips_ps2pdf` profile hardening

### D2. Richer Rendering

- `Future`: true page scene-graph / custom page renderer
- `Future`: SVG/PDF hybrid preview를 넘는 richer render representation
- `Future`: box/glue/page-builder 수준의 actual layout-engine fidelity

### D3. Richer Source / Editor UX

- `Future`: true box-level source attribution
- `Future`: editor-native jump/highlight protocol
- `Future`: richer source map / click-jump / hover-sync surface

### D4. Non-Compatibility Advanced Features

- `Future`: stronger preview diff visualization
- `Future`: revision history / replay debugging UI
- `Future`: artifact explorer for `semantic.aux`, `semantic-index.json`, `build-meta.json`
- `Future`: performance dashboard and regression trend tracking

## Suggested Working Order

1. post-`M12` engine/profile expansion
2. post-`M12` artifact-driven invalidation tightening
3. richer renderer/display-list ownership
4. stronger external editor integration
5. post-compatibility advanced features
